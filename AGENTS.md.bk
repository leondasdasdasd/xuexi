开发与测试统一规范

1. `AGENTS.md` 编辑规范

- 新增或修改 `AGENTS.md` 指令时，必须将每条指令写成正向、可执行的义务，明确触发条件、必须执行的行为和可观察结果
- 必须使用“必须”、“使用”、“保持”、“返回”、“删除”、“仅”等肯定、命令式表达选定的行为和范围
- 修改 `AGENTS.md` 时，必须审查全文中的禁止式、否定式和排除式指令，并将每一处改写为对应的正向行为、范围与结果
- 完成 `AGENTS.md` 修改前，必须运行全文检索并人工复核，交付结果必须保持所有行为指令为正向 prompt

2. 环境与运行

- 项目使用的 Node.js 版本以 `.nvmrc` 为准，执行命令前先运行 `nvm use`
- 本地前后端全链路测试必须使用 `npm run dev:local` 启动当前前端，并确认本地后端同时监听 `8060`；浏览器统一访问 `http://task.local.yungu-inc.org:8000`，让 Vite 本地代理连接后端。
- 默认使用 `npm run start:daily` 启动项目
- 本地访问统一使用 `task.local.yungu-inc.org`
- daily 测试环境地址统一使用 `task.daily.yungu-inc.org`
- 线上环境地址统一使用 `task.yungu.org`

3. 后端联调

- 连接 daily 环境时携带 `.yungu-inc.org` 域 cookie
- 需要获取 cookie 时，登录 `https://login.daily.yungu-inc.org/cas/login`（admin / 123456）获取 cookie

4. 代码质量（强制执行）

- 每次新增代码都必须增加对应的业务逻辑测试；条件允许时，必须同时为相关旧逻辑补充测试
- 测试必须聚焦真实业务行为和可观察结果
- 新增文件必须通过 `npm run lint:changed`
- 每次变更必须维持或减少 `npm run lint` 的问题数量
- 新增代码和新增 React 组件必须优先使用 TypeScript/TSX 实现；旧组件需要大范围重构时，必须同步评估并优先迁移为 TypeScript/TSX
- TypeScript 引用类型尚未明确的旧 JavaScript 模块时，可临时补充 `.d.ts` 声明；声明必须保持窄范围并贴近当前调用边界，使用具体类型表达真实数据 shape
- 每个 JSX/React 组件文件必须只定义一个 React 组件；多个组件协作时，必须按职责拆成独立文件，并通过明确的 props/mapper 传递数据
- 单个文件长度超过 ESLint 要求时，必须按职责和数据流整体拆分，使每个文件对应稳定、清晰的语义边界
- 代码行数超过要求时，必须围绕稳定抽象、数据流和边界完整重构职责，使拆分后的每个模块都具备独立且明确的职责
- 必须原样保留现有 ESLint 和测试质量门槛配置，并通过改进实现满足规则
- 必须通过删除旧代码和过时文档控制技术债务，并统一采用当前契约和当前运行环境
- 各层必须只依赖本层职责和明确边界契约。组件、hook、domain、persistence、service 等层之间的数据必须通过集中 adapter/mapper 显式转换；下层 payload、上层 UI 状态、临时实现字段、后端 DTO、组件 view state 等细节必须由所属边界封装；同一业务概念必须保留一个权威入口
- 同一业务概念、题型行为、保存/提交链路、AI 请求链路和展示状态映射必须各自保留一个权威处理入口；新增行为必须扩展已有数据驱动配置或 registry
- 数据形状跨边界时必须通过集中 adapter/mapper 显式转换；名称必须同时体现来源层、目标层和业务语义，并且每个转换函数必须承担单一边界职责

5. 兼容性与设计缺陷处理

- 必须默认实现当前契约，并删除兼容层、旧接口、旧字段、旧逻辑、fallback 和并行行为
- 旧接口与新接口冲突时，必须统一为一种权威数据结构，并同步迁移所有调用方
- 以下情况必须按设计缺陷处理，并通过重构和删除冗余路径恢复单一权威实现：
  - 同一语义出现多个字段名
  - 同一数据存在多个来源
  - 同一职责存在多层 fallback
  - feature flag 嵌套 feature flag
  - 新旧逻辑同时存在
- 确有当前、明确的业务依赖需要兼容时，必须在实现前记录：
  1. 依赖旧行为的具体调用方、用户或系统
  2. 兼容逻辑的生命周期和截止日期
  3. 删除兼容逻辑的触发条件与负责人
  4. 直接采用当前契约会产生的具体影响
- 上述四项证据齐全后，必须将兼容逻辑集中在所属边界，并添加可追踪的删除计划和覆盖其生命周期的测试

6. 数据形状全链路追踪

- 设计、实现和审查边界变更时，必须追踪数据从入口到最终存储或输出的完整流向，例如：HTTP 输入 → adapter → domain → service → persistence → database
- 必须对每个阶段记录权威数据形状，并显式标出：
  - 字段名变化
  - 类型变化
  - 结构变化
  - 单位、枚举或业务语义变化
  - 兼容分支及其删除计划
- 每次形状变化都必须由所属边界的集中 mapper/adapter 完成，并通过名称和类型签名表达来源、目标和失败语义
- 同一语义出现多个表示方式时，必须将其判定为架构问题并统一为一个权威表示。例如 `questionId → qid → questionCode → question_id` 必须收敛为各层明确约定的单一名称；若传输层或存储层命名确需不同，则必须只在对应边界集中映射一次
- 评估每个问题时必须先提出删除方案，再提出新增方案；在同等满足当前契约和验证要求的前提下，必须采用删除冗余代码更多、权威路径更少的方案

7. 前端调试与测试

- 样式与交互优先使用 Chrome DevTools
- 样式尺寸默认使用响应式/相对单位，优先使用 `rem`、`%`、`fr`、`vw/vh`、`cqw/cqh` 等，使页面能够随根字号、容器尺寸和视口尺寸自动缩放
- 字体、间距、圆角、布局宽高等可伸缩尺寸优先使用 `rem`；栅格布局优先使用 `%`、`fr`、`minmax()`、`clamp()`；组件内部根据容器变化时优先使用 container query 相关单位
- `px` 用于需要像素级精度且尺寸保持固定的场景，例如 `1px` 边框、分割线、图标尺寸、固定控件最小尺寸和第三方组件适配
- 正文文字、主要布局间距和响应式布局使用可缩放的相对单位
- 完成样式或交互修改后，使用 Chrome DevTools 验证桌面、窄桌面、平板和手机宽度下的响应式可读性，并确认横向与纵向内容完整、文字清晰、主要操作按钮可见
- 目标场景需要 Chrome DevTools 之外的补充覆盖时，使用 Playwright 完成自动化验证

8. 注释规范

- 关键路径和关键代码写中文注释，说明业务意图、数据流转、边界条件或实现原因
- 注释聚焦需要补充表达的原因、约束和业务语义

9. 文案规范

- 所有面向用户的文案提供中英双语内容，中文与英文表达同等信息
- 中文和英文分别采用符合母语使用者习惯、语气与上下文的本地化表达
- 文案与界面设计以英文版本作为宽度和布局压力测试基准，确保长英文文案自然换行、完整展示，并为主要操作保留充足空间；中文版本同步达到相同的可读性标准

10. 重复代码扫描与审查

- 前端代码修改后，执行 `npm run duplication:scan`。
- 该命令必须使用 jscpd 生成适合 LLM 分析的重复代码报告，并以识别本次变更相关的高价值重复为目标
- 重复代码修复必须优先处理职责稳定、重复明显、边界清晰、生产影响较大的重复；跨层 shape、生成代码、测试场景样板和合理的 DTO/domain 平行结构必须保持各自边界
- 判断是否抽象时，必须先确认抽象对象、数据流、调用边界和复用收益；抽象会让上层理解下层实现细节时，必须保留重复或选择更窄的边界内复用
- 触达文件必须使用 `git status --short`、`git diff --name-only`、`git diff --cached --name-only` 和 `git ls-files --others --exclude-standard` 综合识别，覆盖未暂存、已暂存和未跟踪新文件
- 必须根据完整 jscpd 结果、实际 diff、完整触达文件和邻近相似实现，判断是否需要由本次任务的共享 review subagent 执行重复度与复杂度评估
- 实际 diff 必须用于识别本次变更意图和重点上下文；重复度判断必须查看 jscpd 报告中的全量重复结果
- Subagent 必须把本次变更放回全量重复结果中判断：哪些重复因本次修改被引入、扩大、依赖、暴露或变得值得立即修复。
- 必须仅要求处理职责稳定、重复明显、边界清晰、生产影响较大且修复后保持层间 shape 和抽象边界的重复；历史重复必须保留为独立的后续治理范围
- Subagent 必须返回 `pass` 或 `fail`；若为 `fail`，必须说明重复文件、重复职责、为什么值得抽象或复用，以及建议修复方向。
- `pass` 必须表示全量重复结果中不存在需要随本次变更处理的高价值重复
- 修复重复时用 `rg` 和/或 `ast-grep` 扫描相似变体，再决定是否复用、抽象或保留。
- 必须优先复用现有 mapper、domain、service、component 边界，并保持同一职责只有一套 helper 路径

11. 任务结束前抽象泄露复查

- 对每个中等或更大的代码任务，最终回复前必须由本次任务的共享 review subagent 执行复查，完整扫描每个被触达文件中新引入的抽象泄露；diff 必须用于识别触达文件和理解变更意图，完整文件内容必须作为复查依据
- 满足任一条件即视为中等或更大的任务：变更多于一个文件；跨模块、层、前后端、运行时或职责边界；变更 public API、schema、DTO、interface、组件 props、hook、service、adapter、store、controller、repository 或其他边界形状；引入或改变抽象、数据流、状态流、请求/响应形状、持久化形状或生命周期契约；经历多轮实现或包含非机械设计选择。
- 单文件、本地、机械的小任务，且原样保留 public boundary、数据形状、抽象和调用约定时，可直接完成本地复查
- subagent 复查必须保持架构中立，并根据当前完整文件内容推断实际边界、数据形状、职责线和调用约定
- subagent 必须从 edits、diff、status 和工具活动中识别触达文件，并阅读每个触达文件的完整当前内容；diff 只作为理解变更意图的线索。
- 触达文件必须使用 `git status --short`、`git diff --name-only`、`git diff --cached --name-only` 和 `git ls-files --others --exclude-standard` 综合识别，覆盖未暂存、已暂存和未跟踪新文件
- subagent 必须检查变更是否迫使调用方或上层理解不该暴露的内部细节，包括框架细节、存储细节、传输细节、组件内部状态、缓存策略、生命周期顺序、错误实现细节、具体容器、协议格式、数据库形状、UI 内部状态或第三方库细节。
- subagent 必须检查是否绕过既有边界、复制既有模式、放错转换逻辑，或为同一职责引入第二套抽象。
- 当前变更引入、加重、依赖或让某个 touched boundary 相关的抽象泄露，都属于本次复查范围。
- 实际 diff 必须用于识别本次变更意图和重点上下文；抽象泄露判断必须基于完整触达文件、当前边界和邻近实现
- Subagent 必须把本次变更放回当前完整代码中判断：哪些抽象泄露因本次修改被引入、扩大、依赖、暴露或变得值得立即修复。
- 必须仅要求处理与本次变更相关、影响当前边界正确性且可在本次范围内修复的抽象泄露；历史抽象泄露必须保留为独立的后续治理范围
- subagent 必须返回 `pass` 或 `fail`。返回 `fail` 时必须指出具体文件、泄露的抽象、为什么这是泄露，以及预期修复方向。
- `pass` 必须表示当前完整触达文件和邻近边界中不存在需要随本次变更处理的抽象泄露
- 主 agent 必须负责修复：先审阅 subagent 结论，再直接在主工作区修复确认存在的抽象泄露；修复后运行最小有意义验证；随后再次运行 subagent 抽象泄露复查。必须重复复查、修复、验证、再复查，直到 subagent 返回 `pass`。
- 必需的 subagent 复查受阻时，最终回复必须明确记录阻塞原因和质量门禁的完成状态

12. Subagent 最佳实践 Review

- 对每个中等或更大的代码任务，在抽象泄露检查通过后，必须由同一个共享 review subagent 执行最佳实践 review
- subagent 必须读取触达文件完整当前内容，并结合实际 diff、邻近实现和本文件规则判断。
- 检查实现是否符合当前技术栈和本项目既有最佳实践，包括数据 shape、层间流向、React 组件拆分、类型约束、测试覆盖、错误处理、可访问性、性能和可维护性。
- 检查是否存在可在本次范围内优化为更佳实践的实现，例如重复状态、过宽 props、命令式数据转换、隐式副作用、脆弱条件分支、过度抽象或缺失边界转换。
- subagent 必须仅要求处理与本次变更相关、收益明确、风险可控且符合现有架构的优化；宽泛重构、风格偏好调整和历史问题必须保留为独立后续范围
- subagent 必须返回 `pass` 或 `fail`。
- 若为 `fail`，必须说明具体文件、问题实现、违反的最佳实践、建议修复方向和为什么应在本次范围内处理。
- `pass` 必须表示当前触达文件中不存在需要随本次变更处理的最佳实践问题
- 如果 subagent 返回 `fail`，主 agent 判断问题是否成立；修复成立的问题；运行最小有效验证；再次请求 subagent 复查；重复直到返回 `pass`。
- 必需的 subagent review 受阻时，最终回复必须明确记录阻塞原因和质量门禁的完成状态

13. 任务完成报告

- 完成任务时必须简要报告：识别到的数据形状和流转；复用的既有代码；新功能工作是否检查了外部资料；是否考虑过引入 package；保留或引入了什么抽象或边界；任务结束前抽象泄露复查是否运行、发现了什么、是否经历修复与再复查；最佳实践 review 是否运行、发现了什么、是否经历修复与再复查；运行了什么验证。
