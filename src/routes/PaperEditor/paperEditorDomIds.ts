export const getPaperQuestionElementId = (questionKey: string) =>
  `paper-question-${questionKey}`;

export const getPaperQuestionScoreElementId = (questionKey: string) =>
  `paper-question-score-${questionKey}`;

export const getPaperModuleTitleElementId = (moduleKey: string) =>
  `paper-module-title-${moduleKey}`;
