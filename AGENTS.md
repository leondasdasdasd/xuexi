开发与测试统一规范

1. 环境与运行

- 项目使用的 Node.js 版本以 .nvmrc 为准，执行命令前先 nvm use
- 默认情况下启动项目使用 `npm run start:daily`
- 本地访问优先使用 task.local.yungu-inc.org（避免 localhost / 127.0.0.1）
- daily 测试环境的地址一般是: task.daily.yungu-inc.org
- 线上环境的地址是: task.yungu.org

2. 后端联调

- 连接 daily 环境必须携带 .yungu-inc.org 域 cookie
- 无 cookie 时登录 https://login.daily.yungu-inc.org/cas/login（admin / 123456）

3. 代码质量（不可修改）

- 每次新增代码都必须增加对应测试；如果可行，也要为相关旧逻辑补充测试
- 测试必须是关于业务逻辑的测试，而不是为了测试而写测试
- 构建、工具链和配置文件不新增单元测试；此类变更必须通过实际运行对应命令验证
- 新增文件必须通过 npm run lint:changed
- npm run lint 数量只能减少不能增加
- 新增代码和新增 React 组件优先使用 TypeScript/TSX 实现；旧组件如果需要大范围重构，也可以改用 TypeScript/TSX 实现。
- TypeScript 引用旧 JavaScript 模块时，如果类型不明确，可以临时补充 `.d.ts` 声明；声明必须尽量窄、贴近当前调用边界，禁止用宽泛 `any` 掩盖真实数据 shape。
- 一个 JSX/React 组件文件只能定义一个 React 组件；需要多个组件协作时，必须按职责拆成独立文件，并通过明确的 props/mapper 传递数据，禁止在同一个 JSX 文件中堆多个组件。
- 如果单个文件长度超过 ESLint 要求，必须按职责和数据流对文件做整体拆分；禁止为了通过限制而做机械式、无语义的拆分。
- 如果代码行数超过要求，必须重构拆分职责，而不是为了压低行数而压低行数；需要拆分时必须按稳定抽象、数据流和边界彻底拆分，禁止只挪几行代码制造形式上的合规。
- 禁止为了通过 ESLint 或测试而修改 ESLint 配置或测试配置文件
- 为避免技术债务，必须删除旧代码与过时文档，不保留向后兼容。
- 绝对不能有抽象泄露：组件、hook、domain、persistence、service 等层只能依赖本层职责和明确边界契约；跨层数据必须通过集中 adapter/mapper 显式转换；禁止把下层 payload、上层 UI 状态、临时实现字段、后端 DTO、组件 view state 或跨层细节透传到不属于它们的层；同一业务概念必须保留一个权威入口，禁止在调用点临时拼装第二套形状。
- 组件内部绝对不能有多条并行处理逻辑：同一业务概念、题型行为、保存/提交链路、AI 请求链路、展示状态映射只能有一个权威入口；新增分支必须优先扩展已有数据驱动配置或 registry，禁止复制一套相似流程。
- 数据形状跨边界时必须通过集中 adapter/mapper 显式转换，命名要体现来源层、目标层和业务语义，禁止使用某一场景的实现名承载多个边界的通用数据转换。

4. 前端调试与测试

- 样式与交互优先使用 Chrome DevTools
- 样式尺寸默认使用响应式/相对单位，优先使用 `rem`、`%`、`fr`、`vw/vh`、`cqw/cqh` 等，使页面能够随根字号、容器尺寸和视口尺寸自动缩放。
- 字体、间距、圆角、布局宽高等可伸缩尺寸优先使用 `rem`；栅格布局优先使用 `%`、`fr`、`minmax()`、`clamp()`；组件内部可根据容器变化时优先考虑 container query 相关单位。
- 只有需要像素级精度且不应随缩放变化的场景才使用 `px`，例如 `1px` 边框、分割线、图标尺寸、固定控件最小尺寸、第三方组件适配等。
- 避免在正文文字、主要布局间距和响应式布局中大量使用固定 `px`。
- 完成样式或交互修改后，需要使用 Chrome DevTools 在多个页面尺寸下验证响应式可读性，至少覆盖桌面、窄桌面、平板和手机宽度，并检查横向溢出、纵向溢出、文字遮挡和主要操作按钮可见性。
- 无法满足时使用 Playwright 做自动化测试

5. 注释规范

- 关键路径和关键代码必须写中文注释，说明业务意图、数据流转、边界条件或非显而易见的实现原因
- 注释应帮助理解为什么这样处理，避免重复描述代码本身

6. 文案规范

- 所有面向用户的文案必须提供中英双语内容，中文与英文应表达同等信息，避免只维护单一语言版本。
- 双语文案不能只是逐字翻译，必须分别符合中文与英文母语使用者的表达习惯、语气和上下文，必要时应进行本地化改写而不是直译。
- 文案与界面设计必须以高标准验收英文版本：优先按英文内容的宽度预留空间，长英文文案不得出现异常换行、遮挡、截断、溢出或挤压主要操作；中文版本不能作为唯一布局基准。

7. 重复代码扫描与审查

- 前端代码修改后，执行 `npm run duplication:scan`。
- 该命令使用 jscpd 生成适合 LLM 分析的重复代码报告，结果只作为分析输入，不要求清零。
- 重复代码修复默认高价值优先：优先处理职责稳定、重复明显、边界清晰、生产影响较大的重复；不为了降低指标强行合并跨层 shape、生成代码、测试场景样板或合理的 DTO/domain 平行结构。
- 判断是否抽象时，先确认抽象对象、数据流、调用边界和复用收益；如果抽象会让上层理解下层实现细节，必须保留重复或选择更窄的边界内复用。
- 触达文件必须用 `git status --short`、`git diff --name-only`、`git diff --cached --name-only` 和 `git ls-files --others --exclude-standard` 综合识别，覆盖未暂存、已暂存和未跟踪新文件；不要依赖 `git diff --stat` 识别触达文件，因为它会漏掉未跟踪文件。
- 根据完整 jscpd 结果、实际 diff、完整触碰文件和邻近相似实现，判断是否需要开独立 subagent 做重复度/复杂度评估。
- 实际 diff 用于识别本次变更意图和重点上下文；重复度判断必须查看 jscpd 报告中的全量重复结果，而不是只看本次 diff 新增的重复片段。
- Subagent 必须把本次变更放回全量重复结果中判断：哪些重复因本次修改被引入、扩大、依赖、暴露或变得值得立即修复。
- 不要求清零历史重复；只有职责稳定、重复明显、边界清晰、生产影响较大，并且修复不会造成跨层 shape 合并或抽象泄露的重复，才应要求本次处理。
- Subagent 必须返回 `pass` 或 `fail`；若为 `fail`，必须说明重复文件、重复职责、为什么值得抽象或复用，以及建议修复方向。
- `pass` 表示全量重复结果中没有需要随本次变更处理的高价值重复，不代表项目重复已清零。
- 修复重复时用 `rg` 和/或 `ast-grep` 扫描相似变体，再决定是否复用、抽象或保留。
- 优先复用现有 mapper、domain、service、component 边界，禁止为同一职责新增第二套 helper 路径。

8. 任务结束前抽象泄露复查

- 对每个中等或更大的代码任务，最终回复前必须运行一次独立 subagent 复查，完整扫描每个被触达文件中新引入的抽象泄露。diff 只能用于识别触达文件和理解变更意图，不能只检查变更行。
- 满足任一条件即视为中等或更大的任务：变更多于一个文件；跨模块、层、前后端、运行时或职责边界；变更 public API、schema、DTO、interface、组件 props、hook、service、adapter、store、controller、repository 或其他边界形状；引入或改变抽象、数据流、状态流、请求/响应形状、持久化形状或生命周期契约；经历多轮实现或包含非机械设计选择。
- 单文件、本地、机械的小任务，且不影响 public boundary、数据形状、抽象或调用约定时，才可以跳过该复查。
- subagent 复查必须保持架构中立：不要预设 frontend、backend、MVC、DDD、React、Spring、Node 或其他固定架构；必须根据当前完整文件内容推断实际边界、数据形状、职责线和调用约定。
- subagent 必须从 edits、diff、status 和工具活动中识别触达文件，并阅读每个触达文件的完整当前内容；diff 只作为理解变更意图的线索。
- 触达文件必须用 `git status --short`、`git diff --name-only`、`git diff --cached --name-only` 和 `git ls-files --others --exclude-standard` 综合识别，覆盖未暂存、已暂存和未跟踪新文件；不要依赖 `git diff --stat` 识别触达文件，因为它会漏掉未跟踪文件。
- subagent 必须检查变更是否迫使调用方或上层理解不该暴露的内部细节，包括框架细节、存储细节、传输细节、组件内部状态、缓存策略、生命周期顺序、错误实现细节、具体容器、协议格式、数据库形状、UI 内部状态或第三方库细节。
- subagent 必须检查是否绕过既有边界、复制既有模式、放错转换逻辑，或为同一职责引入第二套抽象。
- 当前变更引入、加重、依赖或让某个 touched boundary 相关的抽象泄露，都属于本次复查范围。
- 实际 diff 用于识别本次变更意图和重点上下文；抽象泄露判断必须基于完整触碰文件、当前边界和邻近实现，而不是只看本次 diff hunk。
- Subagent 必须把本次变更放回当前完整代码中判断：哪些抽象泄露因本次修改被引入、扩大、依赖、暴露或变得值得立即修复。
- 不要求清理历史抽象泄露；只有和本次变更相关、影响当前边界正确性，并且可在本次范围内修复的抽象泄露，才应要求本次处理。
- subagent 必须返回 `pass` 或 `fail`。返回 `fail` 时必须指出具体文件、泄露的抽象、为什么这是泄露，以及预期修复方向。
- `pass` 表示当前完整触碰文件和邻近边界中没有需要随本次变更处理的抽象泄露，不表示项目不存在历史抽象泄露。
- 主 agent 必须负责修复：先审阅 subagent 结论，再直接在主工作区修复确认存在的抽象泄露；修复后运行最小有意义验证；随后再次运行 subagent 抽象泄露复查。必须重复复查、修复、验证、再复查，直到 subagent 返回 `pass`。
- 如果必需的 subagent 复查无法运行，最终回复必须明确说明，且不能声称抽象泄露质量门禁已通过。

9. Subagent 最佳实践 Review

- 对每个中等或更大的代码任务，在抽象泄露检查通过后，启动独立 subagent 做最佳实践 review。
- subagent 必须读取触达文件完整当前内容，并结合实际 diff、邻近实现和本文件规则判断。
- 检查实现是否符合当前技术栈和本项目既有最佳实践，包括数据 shape、层间流向、React 组件拆分、类型约束、测试覆盖、错误处理、可访问性、性能和可维护性。
- 检查是否存在可在本次范围内优化为更佳实践的实现，例如重复状态、过宽 props、命令式数据转换、隐式副作用、脆弱条件分支、过度抽象或缺失边界转换。
- 不要求做宽泛重构、风格偏好调整或清理历史问题；只有和本次变更相关、收益明确、风险可控且符合现有架构的优化才应要求本次处理。
- subagent 必须返回 `pass` 或 `fail`。
- 若为 `fail`，必须说明具体文件、问题实现、违反的最佳实践、建议修复方向和为什么应在本次范围内处理。
- `pass` 表示当前触达文件中没有需要随本次变更处理的最佳实践问题，不表示项目整体没有可优化空间。
- 如果 subagent 返回 `fail`，主 agent 判断问题是否成立；修复成立的问题；运行最小有效验证；再次请求 subagent 复查；重复直到返回 `pass`。
- 如果无法运行必须的 subagent review，最终回复必须说明原因，且不能声称最佳实践 review 已通过。

10. 任务完成报告

- 完成任务时必须简要报告：识别到的数据形状和流转；复用的既有代码；新功能工作是否检查了外部资料；是否考虑过引入 package；保留或引入了什么抽象或边界；任务结束前抽象泄露复查是否运行、发现了什么、是否经历修复与再复查；最佳实践 review 是否运行、发现了什么、是否经历修复与再复查；运行了什么验证。
