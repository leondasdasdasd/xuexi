# 表单弹窗、表单组件与表格系统开发规范

## 1. 文档目标

本文档基于当前组件库 Figma 设计稿整理，目标是将弹窗、表单控件、列表筛选区与表格页面沉淀为统一、可复用、可维护、可扩展的前端开发规范，并作为 React 19 + TypeScript + Vite + Tailwind CSS v4 / 全局 CSS 项目的统一实现标准。

本文档适用于以下场景：

- 组件库页面
- 业务列表页筛选区
- 表单录入页
- 弹窗表单页
- 表格页
- Figma 到代码还原开发
- 联调、自测、视觉验收

本文档不适用于以下场景：

- 移动端原生表单规范
- 浏览器系统级 `alert / confirm / prompt`
- 纯营销类展示页视觉规范
- 独立于当前组件体系之外的新 UI 框架规范

---

## 2. Figma 参考源

### 2.1 本次规范覆盖的核心节点

| 组件类型             | Figma 节点  | 说明                  |
| -------------------- | ----------- | --------------------- |
| Button               | `404:17167` | 按钮状态页            |
| Search               | `404:16904` | 搜索框状态页          |
| Input                | `404:16861` | 输入框状态页          |
| Textarea             | `404:16932` | 多行输入框状态页      |
| Select               | `421:122`   | 选择器状态页          |
| Dropdown             | `421:175`   | 单选 / 多选下拉状态页 |
| Tabs                 | `423:122`   | Tabs 状态页           |
| Tag                  | `425:2531`  | 标签状态页            |
| Table                | `439:20862` | 表格完整状态页        |
| Summary Card         | `582:5633`  | 概览型卡片列表项      |
| Detail Followup Card | `582:5965`  | 跟进型卡片列表项      |

### 2.2 历史补充节点

若业务弹窗仍使用之前的弹窗设计稿，可继续参考以下 6 个弹窗节点：

| 类型               | Node ID    | 说明                |
| ------------------ | ---------- | ------------------- |
| 导入流程弹窗成功态 | `429:3482` | 表单导入 + 成功反馈 |
| 导入流程弹窗失败态 | `429:3431` | 表单导入 + 失败反馈 |
| 结果反馈成功态     | `429:3532` | 操作成功反馈        |
| 结果反馈失败态     | `429:3564` | 操作中断 / 失败反馈 |
| 提示弹窗           | `429:3553` | 温馨提示            |
| 危险确认弹窗       | `429:3542` | 删除确认            |

---

## 3. 组件映射约束

根据项目实现约束，以下 Figma 组件必须优先映射为现有组件，不允许脱离组件库重新手写一套视觉相似但实现割裂的组件。

| Figma 组件 | 项目实现路径                                            | 映射规则                                                   |
| ---------- | ------------------------------------------------------- | ---------------------------------------------------------- |
| Button     | `src/components/table-system/Button.tsx`                | `primary / secondary / tertiary / quaternary / ghost`      |
| Search     | `src/components/table-system/SearchField.tsx`           | 左侧固定搜索图标，支持清空                                 |
| Input      | `src/components/table-system/InputField.tsx`            | 支持 `default / hover / focus / filled / error / disabled` |
| Select     | `src/components/table-system/SelectField.tsx`           | 单选选择器，自定义浮层菜单                                 |
| Dropdown   | `src/components/table-system/DropdownField.tsx`         | 单选 / 多选下拉，自定义菜单                                |
| Textarea   | `src/components/table-system/TextAreaField.tsx`         | 固定字符计数器                                             |
| Tabs       | `src/components/table-system/Tabs.tsx`                  | 一级线型 / 二级分段控件                                    |
| Tag        | `src/components/table-system/Tag.tsx`                   | 状态标签                                                   |
| Table      | `src/components/table-system/TableView.tsx`             | 工具栏 + 表格 + 分页                                       |
| Card List  | `src/components/card-list-system/CardListTemplates.tsx` | 概览卡片 / 跟进卡片模板                                    |

补充要求：

- 图标统一使用内联 SVG。
- 图标颜色统一使用 `currentColor`。
- 禁止直接使用浏览器原生 `<select>` 作为最终交互形态。
- 禁止在业务页面复制一份基础组件样式单独维护。

---

## 4. 设计系统原则

### 4.1 核心原则

- 禁止在业务组件中随意新增与组件库不一致的颜色、圆角、阴影、字号。
- 禁止为了“看起来差不多”重新造一套 Button、Input、Select、Dropdown、Tabs、Tag。
- 所有基础控件必须具备真实交互，不允许只做静态状态图。
- 视觉规则优先沉淀为全局 Token 或组件 Token。
- 弹窗、列表页、表格页中的基础控件必须保持统一视觉语言。
- 组件库展示页可使用固定宽度展示状态；业务页面中必须遵循容器自适应规则。

### 4.2 还原优先级

Figma 当前节点视觉 > 本文档明确规则 > 历史页面经验值

若历史实现与当前 Figma 节点冲突，以当前 Figma 节点和本文档规则为准。

---

## 5. 基础 Token

### 5.1 字体族

| 语义     | 值                    |
| -------- | --------------------- |
| 主字体   | `Alibaba PuHuiTi 3.0` |
| 一级回退 | `PingFang SC`         |
| 二级回退 | `Hiragino Sans GB`    |
| 三级回退 | `Microsoft YaHei`     |
| 通用回退 | `sans-serif`          |

推荐声明：

```css
font-family:
  "Alibaba PuHuiTi 3.0", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
  sans-serif;
```

### 5.2 颜色 Token

| 语义        | 色值      | 用途                                 |
| ----------- | --------- | ------------------------------------ |
| 品牌主色    | `#0445fc` | 主按钮、选中、焦点、链接强调         |
| 品牌 Hover  | `#005BC4` | 主按钮 Hover                         |
| 品牌 Active | `#004493` | 主按钮 Active                        |
| 品牌浅底    | `#E6F1FE` | 选中浅底、浅蓝标签、选项 Hover       |
| 品牌更浅底  | `#F2F6FF` | 表格 Hover、筛选轻交互背景           |
| 品牌弱化    | `#99C7FB` | 禁用蓝色态                           |
| 主标题色    | `#01113D` | 页面标题、表头、输入内容、正文主文案 |
| 次级正文色  | `#27345A` | 次标题、重点说明文本                 |
| 辅助正文色  | `#5A6481` | 描述文案、帮助文案、空状态           |
| 占位文案色  | `#8C94A7` | Placeholder、未选中占位              |
| 禁用文案色  | `#BFC3CE` | Disabled 文案                        |
| 默认边框色  | `#E6E7EC` | 输入框、表格、容器边框               |
| 弱分割线    | `#EDEEF1` | 模块分隔                             |
| 页面浅底    | `#F7F8F9` | 表头、浅容器背景                     |
| 白色        | `#FFFFFF` | 表单容器、表格背景、弹窗容器         |
| 错误色      | `#EF4437` | Error 边框、错误文案                 |
| 错误强调红  | `#FF6559` | 高风险操作链接                       |
| 成功色      | `#16A34A` | Success 状态                         |
| 成功浅底    | `#EBF9F0` | Success 浅背景                       |
| 警告色      | `#EA580C` | Warning 文案 / 图标                  |
| 警告浅底    | `#FFF7ED` | Warning 背景                         |
| 提示信息色  | `#0891B2` | Info 提示                            |
| 品牌链接色  | `#032E92` | 表格操作列默认链接                   |

### 5.3 圆角 Token

| 语义           | 值     |
| -------------- | ------ |
| 弹窗容器圆角   | `24px` |
| 表格容器圆角   | `11px` |
| 输入类控件圆角 | `10px` |
| 小标签圆角     | `6px`  |
| 中等胶囊圆角   | `12px` |
| 浮层圆角       | `14px` |
| 大面板圆角     | `16px` |

### 5.4 阴影 Token

| 语义                       | 值                                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------------- |
| Dropdown / Select 浮层阴影 | `0 14px 28px rgba(0, 0, 0, 0.08), 0 -6px 12px rgba(0, 0, 0, 0.03), 0 2px 8px rgba(0, 0, 0, 0.06)` |
| Modal 阴影                 | `0 20px 40px rgba(0, 0, 0, 0.12), 0 8px 16px rgba(0, 0, 0, 0.08)`                                 |
| 激活胶囊阴影               | `0 2px 6px rgba(1, 17, 61, 0.08)`                                                                 |

### 5.5 间距 Token

建议统一使用以下间距尺度：

| Token       | 值     |
| ----------- | ------ |
| `--space-1` | `4px`  |
| `--space-2` | `8px`  |
| `--space-3` | `12px` |
| `--space-4` | `16px` |
| `--space-5` | `20px` |
| `--space-6` | `24px` |
| `--space-7` | `32px` |
| `--space-8` | `40px` |
| `--space-9` | `48px` |

### 5.6 层级 Token

本节用于统一页面结构层、吸附层、下拉层、浮层、弹窗层、消息层之间的层级关系，避免 Dropdown 盖到 Modal 上面、Tooltip 被 Popover 遮挡、Sticky 标题栏压住下拉面板，以及业务页面自行写 `z-index: 9999` 等问题。

#### 5.6.1 层级 Token 总表

| 语义       | Token 名称          | 值     | 用途                              |
| ---------- | ------------------- | ------ | --------------------------------- |
| 页面基础层 | `--z-base`          | `0`    | 普通文档流内容、卡片、表单、表格  |
| 吸附层     | `--z-sticky`        | `10`   | Sticky 顶栏、右侧辅助区、吸附头部 |
| 下拉层     | `--z-dropdown`      | `1000` | Select / Dropdown 面板            |
| 菜单浮层   | `--z-popover`       | `1100` | MoreMenu / Popover / 轻量菜单     |
| 提示层     | `--z-tooltip`       | `1200` | Tooltip                           |
| 弹窗遮罩层 | `--z-modal-overlay` | `1300` | Modal Overlay                     |
| 弹窗内容层 | `--z-modal`         | `1310` | Modal Surface                     |
| 全局消息层 | `--z-toast`         | `1400` | Toast / Message / 全局轻提示      |

#### 5.6.2 使用规则

- 页面普通内容统一使用 `--z-base`，默认不额外设置 `z-index`。
- Sticky 容器统一使用 `--z-sticky`，仅用于吸附型页面结构，不得承担浮层职责。
- Select / Dropdown 面板统一使用 `--z-dropdown`。
- MoreMenu / Popover 统一高于 Dropdown。
- Tooltip 统一高于 Dropdown 与 Popover。
- Modal 必须整体高于所有页内浮层。
- Toast / Message 默认高于 Modal，用于全局反馈。

#### 5.6.3 强制约束

- 禁止在业务页面直接写 `z-index: 9999`、`999`、`99999` 等无体系层级值。
- Dropdown 不得覆盖 Modal 之上。
- Tooltip 不得被 Sticky 栏或表格固定列遮挡。
- Sticky 表头、吸附区如需和表格固定列共存，仍必须低于 Dropdown 和 Modal。
- 同一页面中若存在多层浮层嵌套，必须以本节 Token 为唯一依据。

#### 5.6.4 特殊场景规则

##### A. Modal 内部的 Dropdown / Popover

当 Select、Dropdown、MoreMenu 出现在 Modal 内部时：

- 其视觉层级必须高于当前 Modal 内容区内部普通元素；
- 但仍应被视为 Modal 体系内部浮层；
- 不得越过更高优先级的全局弹窗或全局消息层。

##### B. Table 固定列 / Sticky Header

- 表格固定列、Sticky Header 只允许高于普通表格内容；
- 不得高于 Dropdown / Tooltip / Modal；
- 固定列阴影与 Hover 叠加时，不得通过额外提高 `z-index` 强行解决。

#### 5.6.5 验收标准

- 不会再出现“弹窗下面的下拉盖到弹窗上面”的问题。
- 不会再出现 Tooltip 被 Sticky Header 截断。
- 不会再出现不同页面各自写一套 `z-index`。

### 5.7 边框 / 透明度 / 图标 Token

本节用于统一边框宽度、弱化透明度与图标尺寸，避免同样的边框有的写 `1px`、有的写 `1.5px`，Disabled 有的用颜色弱化、有的直接整体透明，以及同类图标出现 `15px`、`17px` 等不规则尺寸。

#### 5.7.1 边框 Token

| 语义     | Token 名称               | 值    | 用途                       |
| -------- | ------------------------ | ----- | -------------------------- |
| 默认边框 | `--border-width-default` | `1px` | 输入框、表格、卡片、分割线 |
| 强调边框 | `--border-width-strong`  | `2px` | 特殊强调、焦点外框辅助态   |

补充规则：

- 绝大多数容器、字段、分割线统一使用 `1px`。
- `2px` 仅用于极少数强调场景，不得滥用。
- 禁止业务页面使用 `1.5px`、`3px` 等非系统边框宽度。

#### 5.7.2 透明度 Token

| 语义              | Token 名称           | 值     | 用途                       |
| ----------------- | -------------------- | ------ | -------------------------- |
| Disabled 弱化辅助 | `--opacity-disabled` | `0.4`  | 图标、非主文案弱化辅助     |
| Overlay 遮罩      | `--opacity-overlay`  | `0.5`  | Modal Overlay              |
| Subtle 轻叠加     | `--opacity-subtle`   | `0.08` | 轻背景、轻阴影、弱高亮辅助 |

补充规则：

- Disabled 态优先依赖颜色 Token，不建议直接把整个控件整体降透明。
- 文本、输入值、表单内容不应通过整体 `opacity` 直接弱化，否则可读性会下降。
- Overlay 统一使用本节透明度，不允许页面自行发明新的遮罩深度。

强制约束：

- 禁止把整段正文、整张表单卡片直接设置 `opacity: 0.5` 作为禁用态。
- 禁止不同页面出现多个近似但不一致的遮罩透明度。

#### 5.7.3 图标尺寸 Token

| 语义     | Token 名称       | 值     | 用途                                  |
| -------- | ---------------- | ------ | ------------------------------------- |
| 超小图标 | `--icon-size-xs` | `12px` | Tag 内小图标、辅助型状态点缀          |
| 小图标   | `--icon-size-sm` | `14px` | Button 图标、Select 勾选图标、Chevron |
| 中图标   | `--icon-size-md` | `16px` | Search 图标、字段清空图标             |
| 大图标   | `--icon-size-lg` | `20px` | Modal 关闭图标、结果反馈图标          |

补充规则：

- 同一组件内部图标尺寸必须统一。
- 同类组件图标优先复用同一个 Token。
- 图标默认使用 `currentColor`。
- 图标大小不应依赖视觉微调写出 `15px`、`17px`。

推荐映射：

| 场景                      | 推荐尺寸         |
| ------------------------- | ---------------- |
| Button 左图标             | `14px`           |
| Select / Dropdown Chevron | `14px`           |
| 清空按钮图标              | `14px` 或 `16px` |
| Search 图标               | `16px`           |
| Modal 关闭图标            | `20px`           |
| Tag 内图标                | `12px` 或 `14px` |

#### 5.7.4 验收标准

- 不会出现同类图标有的 `15px`、有的 `17px`。
- Disabled 的弱化逻辑一致，不会有的靠透明、有的靠颜色、有的两者混用失控。
- 边框宽度在系统中保持统一。

### 5.8 断点 Token

本节用于统一页面宽度断点、Toolbar 换行断点、表格横向滚动触发点、卡片列表布局切换点，避免文档里只有“空间不足时换行”但没有定义“什么时候算不足”。

#### 5.8.1 断点 Token 总表

| 语义     | Token 名称        | 值       | 说明                         |
| -------- | ----------------- | -------- | ---------------------------- |
| 宽屏桌面 | `--breakpoint-xl` | `1440px` | 大屏办公场景                 |
| 常规桌面 | `--breakpoint-lg` | `1280px` | 默认桌面 B 端主体验          |
| 窄桌面   | `--breakpoint-md` | `1024px` | 小屏笔记本 / 收窄窗口        |
| 过渡区间 | `--breakpoint-sm` | `768px`  | 不建议承载完整高密度表格体验 |

#### 5.8.2 页面级断点策略

##### A. `>= 1280px`

- 优先保持完整桌面布局。
- Toolbar 筛选区优先单行。
- 表格按完整列宽策略展示。
- 卡片列表可使用“主列表 + 右侧辅助区”双栏布局。
- 页面主工作区可按现有 `1272px` 左右设计基准还原。

##### B. `1024px - 1279px`

- 允许 Toolbar 筛选区换行。
- 低频筛选项可进入“更多筛选”。
- 表格优先启用横向滚动，而不是无限压缩列宽。
- 卡片列表右侧辅助区应优先收窄，必要时下移。
- 双列表单仍可保留，但应检查字段宽度是否低于最小阈值。

##### C. `768px - 1023px`

- 不建议强行保持完整桌面骨架。
- Toolbar 应优先折叠低频筛选。
- 表格应允许明确横向滚动。
- 详情头部操作区应优先减少同时可见操作数量。
- 双列表单原则上降级为单列。

##### D. `< 768px`

- 不属于本文主规范覆盖范围。
- 如需支持，必须单独制定移动端 / 小屏端规范。

#### 5.8.3 Toolbar 换行与折叠规则

##### 模式 A：列表筛选型 Toolbar

###### `>= 1280px`

- 优先单行展示高频筛选项。
- Search / Input / Select / Dropdown 保持自适应宽度。
- 批量操作区与工具操作区完整保留。

###### `1024px - 1279px`

- 允许第二行出现。
- 低频筛选项进入“更多筛选”。
- 主查询条件优先保留在首行。

###### `< 1024px`

- 不建议继续横向堆满所有筛选项。
- 应使用“更多筛选”统一收纳低频字段。
- 查询、重置、主操作按钮仍需稳定可见。

#### 5.8.4 表格横向滚动触发规则

当满足以下任一条件时，应优先启用横向滚动：

1. 表格所有列推荐最小宽度总和大于容器宽度。
2. 操作列开始被正文列挤压。
3. 状态列、开关列、头像列等结构列出现压缩变形。
4. 表头或单元格文本开始被无规则截断。

补充规则：

- 表格不得通过无限压缩每列宽度来回避滚动。
- 操作列、选择列、状态列、开关列优先保持结构稳定。
- 横向滚动优先于结构变形。

#### 5.8.5 卡片列表断点规则

##### `>= 1280px`

- 允许“主列表 + 右侧辅助区”。
- 辅助区推荐宽度 `320px`。

##### `1024px - 1279px`

- 辅助区可收窄。
- 主列表优先保留。

##### `< 1024px`

- 右侧辅助区建议下移到底部。
- 不建议继续保持双栏并排压缩。

#### 5.8.6 表单双列降级规则

- 双列表单中，若任一列实际宽度小于 `220px`，必须降级为单列。
- 含日期、年月、组织名称、复杂下拉字段的表单，更应优先单列。
- 不能为了“看起来像双列”而让字段内部出现省略过度、换行、错误文案挤压。

#### 5.8.7 验收标准

- 响应式不是凭感觉，而是有明确断点。
- 不同页面在收窄时行为一致。
- 表格不会为了保持“无滚动”而压坏列结构。
- 筛选区不会在不同实现里各自乱换行。

### 5.9 语义 Token

本节用于在现有基础颜色 Token 之上，建立一层语义别名 Token，避免组件里直接写十六进制色值，以及后续换主题或换品牌时需要全篇查找替换。

#### 5.9.1 分层原则

颜色 Token 分为两层：

##### A. 基础 Token（Foundation Token）

即当前文档中已经定义的原始色值，例如品牌主色 `#0445fc`、主标题色 `#01113D`、占位文案色 `#8C94A7`、默认边框色 `#E6E7EC`。

##### B. 语义 Token（Semantic Token）

在基础 Token 之上，按语义重新命名，供组件和页面直接使用，例如：

- `text-primary`
- `text-secondary`
- `text-placeholder`
- `border-default`
- `border-focus`
- `bg-surface`
- `bg-hover`
- `bg-selected`
- `danger-default`

使用原则：

- 文档层可以保留基础 Token 与色值说明。
- 组件实现层应优先消费语义 Token。
- 页面业务层不得直接写十六进制色值。
- 组件 Token 可继续在语义 Token 上二次封装。

#### 5.9.2 文本语义 Token

| 语义     | Token 名称           | 映射值    | 用途                             |
| -------- | -------------------- | --------- | -------------------------------- |
| 主文本   | `--text-primary`     | `#01113D` | 页面标题、正文、输入值、表格内容 |
| 次主文本 | `--text-secondary`   | `#27345A` | 次级强调、Label、分组标题        |
| 辅助文本 | `--text-tertiary`    | `#5A6481` | 描述、帮助、空状态说明           |
| 占位文本 | `--text-placeholder` | `#8C94A7` | Placeholder                      |
| 禁用文本 | `--text-disabled`    | `#BFC3CE` | Disabled                         |
| 反白文本 | `--text-inverse`     | `#FFFFFF` | 深底按钮、深色状态块上的文字     |
| 链接文本 | `--text-link`        | `#032E92` | 操作链接                         |
| 危险文本 | `--text-danger`      | `#EF4437` | 错误、删除、风险说明             |
| 成功文本 | `--text-success`     | `#16A34A` | Success                          |
| 警告文本 | `--text-warning`     | `#EA580C` | Warning                          |
| 信息文本 | `--text-info`        | `#0891B2` | Info                             |

#### 5.9.3 边框语义 Token

| 语义       | Token 名称          | 映射值    | 用途                          |
| ---------- | ------------------- | --------- | ----------------------------- |
| 默认边框   | `--border-default`  | `#E6E7EC` | 输入框、卡片、表格、弹窗      |
| 弱分割线   | `--border-muted`    | `#EDEEF1` | 模块分割、轻分隔              |
| Hover 边框 | `--border-hover`    | `#CFCFD6` | Input / Search / Select Hover |
| 焦点边框   | `--border-focus`    | `#0445fc` | Focus / Open                  |
| 错误边框   | `--border-danger`   | `#EF4437` | Error                         |
| 禁用边框   | `--border-disabled` | `#E6E7EC` | Disabled 场景                 |

#### 5.9.4 背景语义 Token

| 语义       | Token 名称           | 映射值    | 用途                     |
| ---------- | -------------------- | --------- | ------------------------ |
| 页面背景   | `--bg-page`          | `#F7F8F9` | 页面浅底、浅容器背景     |
| 表面背景   | `--bg-surface`       | `#FFFFFF` | 卡片、弹窗、表格、输入框 |
| Hover 背景 | `--bg-hover`         | `#F2F6FF` | 表格 Hover、轻交互 Hover |
| 选中背景   | `--bg-selected`      | `#E6F1FE` | 已选项、品牌浅底         |
| 禁用背景   | `--bg-disabled`      | `#F2F3F5` | Disabled 控件            |
| 浅弱背景   | `--bg-subtle`        | `#F7F8F9` | 次级按钮、Tabs 容器      |
| 成功背景   | `--bg-success`       | `#EBF9F0` | Success 浅底             |
| 警告背景   | `--bg-warning`       | `#FFF7ED` | Warning 浅底             |
| 信息背景   | `--bg-info`          | `#E8F7FB` | Info 浅底                |
| 危险背景   | `--bg-danger-subtle` | `#FDECEC` | Danger 浅底              |

#### 5.9.5 品牌与交互语义 Token

| 语义        | Token 名称                  | 映射值    | 用途                 |
| ----------- | --------------------------- | --------- | -------------------- |
| 品牌主交互  | `--action-primary`          | `#0445fc` | 主按钮、主选中、焦点 |
| 品牌 Hover  | `--action-primary-hover`    | `#005BC4` | 主按钮 Hover         |
| 品牌 Active | `--action-primary-active`   | `#004493` | 主按钮 Active        |
| 品牌弱化    | `--action-primary-disabled` | `#99C7FB` | 主按钮 Disabled      |
| 品牌浅底    | `--action-primary-subtle`   | `#E6F1FE` | 轻选中、轻按钮       |
| 危险主交互  | `--action-danger`           | `#EF4437` | 危险确认、错误强调   |
| 危险 Hover  | `--action-danger-hover`     | `#DC2626` | 危险态 Hover         |

#### 5.9.6 图标语义 Token

| 语义     | Token 名称           | 映射值    | 用途               |
| -------- | -------------------- | --------- | ------------------ |
| 主图标   | `--icon-primary`     | `#01113D` | 默认主图标         |
| 次图标   | `--icon-secondary`   | `#5A6481` | 辅助图标           |
| 占位图标 | `--icon-placeholder` | `#8C94A7` | Search 默认图标等  |
| 品牌图标 | `--icon-brand`       | `#0445fc` | Focus / 选中态图标 |
| 禁用图标 | `--icon-disabled`    | `#BFC3CE` | Disabled           |
| 危险图标 | `--icon-danger`      | `#EF4437` | 删除、错误         |
| 成功图标 | `--icon-success`     | `#16A34A` | Success            |
| 警告图标 | `--icon-warning`     | `#EA580C` | Warning            |
| 信息图标 | `--icon-info`        | `#0891B2` | Info               |

#### 5.9.7 Overlay / Shadow / Focus 语义 Token

| 语义     | Token 名称           | 映射值                     | 用途                           |
| -------- | -------------------- | -------------------------- | ------------------------------ |
| 遮罩背景 | `--overlay-default`  | `rgba(0, 0, 0, 0.5)`       | Modal Overlay                  |
| 浮层阴影 | `--shadow-floating`  | Dropdown / Select 浮层阴影 | Dropdown / Popover             |
| 弹窗阴影 | `--shadow-modal`     | Modal 阴影                 | Modal                          |
| 焦点强调 | `--focus-ring-color` | `#0445fc`                  | 焦点外框、focus-visible 辅助态 |

#### 5.9.8 命名规则

推荐命名格式：

```txt
--{类别}-{语义}
```

例如：

- `--text-primary`
- `--border-focus`
- `--bg-surface`
- `--action-primary`
- `--icon-disabled`

类别建议：

- `text`
- `border`
- `bg`
- `action`
- `icon`
- `overlay`
- `shadow`
- `focus`

#### 5.9.9 使用规则

- 组件实现优先使用语义 Token，不直接写十六进制。
- 业务页面不得直接定义新的“近似语义色”。
- 若某个组件需要组件专属 Token，应建立在语义 Token 之上。

例如：

```css
--button-primary-bg: var(--action-primary);
--button-primary-text: var(--text-inverse);
--input-border-focus: var(--border-focus);
```

- 语义 Token 优先表达“这个颜色干什么用”，而不是“它看起来像哪种蓝”。

#### 5.9.10 禁止项

- 禁止在业务组件里直接写 `#0445fc`、`#01113D` 等色值。
- 禁止一个语义对应多个近似颜色，导致“主文案有三种深蓝”。
- 禁止把同一个颜色在不同组件里命名成不同角色但无明确语义差异。
- 禁止先写组件私有色，后补语义 Token。

#### 5.9.11 验收标准

- 设计和开发可以先讨论“主文本 / 边框默认 / 焦点边框 / Hover 背景”，再讨论具体色值。
- 后续若换主题、换品牌、调色，不需要全篇查找十六进制。
- 组件实现层不再直接依赖原始色值。

### 5.10 Token 使用总原则

- 颜色、层级、边框、图标、断点必须统一通过 Token 管理。
- 页面和组件只能消费 Token，不得绕过 Token 直接写零散数值。
- 基础 Token 负责“原始值”，语义 Token 负责“用途”，组件 Token 负责“组件内部映射”。
- 当基础 Token、语义 Token、组件 Token 冲突时，优先检查是否语义定义错误，不允许业务页面直接覆盖。

---

## 6. 字体系统规范

本章节为新增统一字体规范，必须与颜色、尺寸、间距规则一起执行，不允许页面自行定义与组件库不一致的字号、字重、文字颜色。

### 6.1 字体使用原则

- 中文界面统一使用 `Alibaba PuHuiTi 3.0` 体系。
- 所有标题、正文、表头、表格内容、按钮、输入文字、提示文案都必须落在本文定义的字阶中。
- 禁止同一层级出现多个接近但不一致的字号，例如 `13px / 14px / 15px` 混用。
- 禁止表头使用粗体 `600 / 700`，除非本规范明确说明。
- 列表页、弹窗页、表单页、组件库页必须共用同一套字阶。

### 6.2 字体等级总表

| 文本等级                  | 字号   | 行高   | 字重  | 颜色      | 用途                           |
| ------------------------- | ------ | ------ | ----- | --------- | ------------------------------ |
| Display / 页面主标题      | `24px` | `32px` | `600` | `#01113D` | 页面主标题、组件库总标题       |
| Section Title / 模块标题  | `18px` | `26px` | `600` | `#01113D` | 区块标题、卡片标题             |
| Dialog Title / 弹窗标题   | `16px` | `24px` | `500` | `#01113D` | 弹窗 Header 标题               |
| Emphasis Body / 强调正文  | `14px` | `22px` | `500` | `#27345A` | 表单 Label、步骤标题、强调描述 |
| Body / 常规正文           | `14px` | `22px` | `400` | `#01113D` | 输入内容、表格内容、普通正文   |
| Secondary Body / 次级正文 | `14px` | `22px` | `400` | `#5A6481` | 辅助说明、空状态说明           |
| Caption / 辅助说明        | `12px` | `20px` | `400` | `#5A6481` | 帮助文案、计数器、Tag 文案     |
| Small Emphasis / 小号强调 | `12px` | `20px` | `500` | `#27345A` | 次要强调、状态说明             |

### 6.3 页面与区块文字规范

| 场景              | 字号   | 行高   | 字重  | 颜色      |
| ----------------- | ------ | ------ | ----- | --------- |
| 页面主标题        | `24px` | `32px` | `600` | `#01113D` |
| 页面副标题 / 描述 | `14px` | `22px` | `400` | `#5A6481` |
| 区块标题          | `18px` | `26px` | `600` | `#01113D` |
| 区块辅助说明      | `14px` | `22px` | `400` | `#5A6481` |
| 卡片标题          | `16px` | `24px` | `500` | `#01113D` |
| 分组标题          | `14px` | `22px` | `500` | `#27345A` |

### 6.4 表单文字规范

| 场景                             | 字号   | 行高   | 字重  | 颜色      |
| -------------------------------- | ------ | ------ | ----- | --------- |
| Label                            | `14px` | `22px` | `500` | `#27345A` |
| 必填星号                         | `14px` | `22px` | `500` | `#EF4437` |
| Input / Search / Select 已填内容 | `14px` | `22px` | `400` | `#01113D` |
| Placeholder                      | `14px` | `22px` | `400` | `#8C94A7` |
| Disabled 内容                    | `14px` | `22px` | `400` | `#BFC3CE` |
| Error 文案                       | `12px` | `20px` | `400` | `#EF4437` |
| Helper 文案                      | `12px` | `20px` | `400` | `#5A6481` |
| 字数计数器                       | `12px` | `20px` | `400` | `#5A6481` |

### 6.5 按钮文字规范

| 按钮类型  | 字号         | 行高         | 字重  | 颜色           |
| --------- | ------------ | ------------ | ----- | -------------- |
| `md` 按钮 | `14px`       | `20px`       | `500` | 按变体定义     |
| `sm` 按钮 | `12px`       | `18px`       | `500` | 按变体定义     |
| 图标按钮  | 不单独设字号 | 继承按钮字阶 | `500` | 以图标颜色为准 |

### 6.6 Tabs / Tag / 表格文字规范

| 场景                | 字号   | 行高   | 字重  | 颜色                       |
| ------------------- | ------ | ------ | ----- | -------------------------- |
| Primary Tabs `md`   | `14px` | `22px` | `500` | `#5A6481` / 激活 `#0445fc` |
| Primary Tabs `lg`   | `16px` | `24px` | `500` | `#5A6481` / 激活 `#0445fc` |
| Secondary Tabs `md` | `14px` | `22px` | `500` | `#5A6481` / 激活 `#0445fc` |
| Tag                 | `12px` | `20px` | `400` | 按 tone 定义               |
| 表头文字            | `14px` | `20px` | `400` | `#01113D`                  |
| 表格单元格文字      | `14px` | `20px` | `400` | `#01113D`                  |
| 操作列链接          | `14px` | `20px` | `400` | `#032E92`                  |
| 危险操作链接        | `14px` | `20px` | `400` | `#FF6559`                  |
| 空状态文本          | `14px` | `20px` | `400` | `#5A6481`                  |

### 6.7 文字实现约束

- 表头字体必须使用 `font-weight: 400`，禁止使用粗体表头。
- 表格正文默认使用 `font-weight: 400`。
- 按钮文字统一使用 `font-weight: 500`，不允许某些按钮突然变成 `600`。
- Input、Select、Dropdown、Textarea 的内容文字统一使用 `14px / 22px / 400`。
- Placeholder 必须显著弱于输入值，统一使用 `#8C94A7`。
- Disabled 文案必须统一使用 `#BFC3CE`。

---

## 7. 状态命名规范

### 7.1 字段类状态

适用于 `Search / Input / Select / Dropdown / Textarea`

- `default`
- `hover`
- `focus`
- `filled`
- `error`
- `disabled`

### 7.2 按钮类状态

适用于 `Button`

- `default`
- `hover`
- `active`
- `disabled`

### 7.3 状态语义

- `default`：默认静态状态
- `hover`：鼠标悬停反馈
- `focus`：焦点或菜单激活态
- `filled`：已有输入值或已选值
- `error`：校验失败态
- `disabled`：禁用不可操作态
- `active`：按钮按下态

---

## 8. Form Field 通用规范

本章节用于统一约束所有表单字段类组件的结构、尺寸、间距、宽度、换行、文本、状态、反馈、对齐与布局行为。目标不是只规范“长什么样”，而是规范在真实业务场景里如何稳定地展示、不炸布局、不乱跳、不换行、不挤压。

适用于以下组件：

- Search
- Input
- Select
- Dropdown
- Textarea
- Checkbox Group
- Radio Group
- Switch Field
- Date / Month / Time 类单值字段
- 后续所有复用表单字段

### 8.1 设计目标

Form Field 通用规范必须解决以下问题：

1. 字段标题、控件、反馈文案的上下关系统一。
2. 同一表单区的字段高度、间距、节奏统一。
3. 单值字段内容不得因宽度不足而换行。
4. 图标、文本、清空按钮、下拉箭头不得互相挤压错位。
5. 错误文案出现时，布局变化必须可控。
6. 双列表单在宽度不足时必须有明确降级策略。
7. 列表筛选区、弹窗表单区、详情编辑区必须共享同一字段骨架规则。
8. `readonly`、`disabled`、`error`、`loading` 必须有明确区别。

### 8.2 字段统一结构模型

所有字段统一抽象为以下三层结构：

1. `Label Row`
2. `Control Row`
3. `Message Row`

结构说明：

- `Label Row`：字段标题、必填标识、可选标识。
- `Control Row`：字段本体。
- `Message Row`：Helper / Error / Warning / Success，仅显示一种主反馈。

推荐 DOM 职责：

```txt
Field
├─ Label Row
├─ Control Row
└─ Message Row
```

补充规则：

- 不允许某些字段把错误文案放在控件右侧，另一些放控件下方。
- 不允许同一表单中一部分字段有 Message Row，一部分字段没有预留，导致出现错误时上下跳动明显。
- 高密度固定高度弹窗表单，建议默认预留 `Message Row`。

### 8.3 垂直节奏与占高规范

#### A. 标准表单字段高度（推荐默认）

适用于：弹窗表单、详情编辑区、抽屉表单、标准录入页。

| 区块                    | 高度   |
| ----------------------- | ------ |
| Label Row 最小高度      | `22px` |
| Label 与 Control 间距   | `4px`  |
| Control Row 高度        | `32px` |
| Control 与 Message 间距 | `4px`  |
| Message Row 最小高度    | `20px` |

因此，一个标准字段完整占高为：

`22 + 4 + 32 + 4 + 20 = 82px`

#### B. Toolbar 筛选字段高度

适用于：列表页筛选区、表格页 Toolbar、头部轻筛选区。

| 区块             | 高度   |
| ---------------- | ------ |
| Label Row        | 默认无 |
| Control Row 高度 | `36px` |
| Message Row      | 默认无 |

因此，Toolbar 单字段默认占高：

`36px`

#### C. 不预留 Message Row 的表单字段

若业务明确不需要预留错误文案位置，可使用简化占高：

`22 + 4 + 32 = 58px`

补充规则：

- 标准录入表单优先使用预留 Message Row 的 `82px` 方案。
- 只有在字段非常少、表单高度不敏感、并且错误展示走表单级汇总时，才允许不预留。
- 同一表单区不得混用“有的预留、有的不预留”。

### 8.4 Label Row 规范

#### A. 尺寸与文字

- 最小高度：`22px`
- 文字：`14px / 22px / 500 / #27345A`
- 必填星号：`14px / 22px / 500 / #EF4437`
- Label 默认左对齐。
- Label 与控件之间固定间距：`4px`。

#### B. 布局规则

- Label Row 使用 `display: flex; align-items: center; gap: 4px;`。
- Label 文案默认单行展示。
- 不建议 Label 自然换成两行。
- 若文案过长，优先：
  1. 优化文案。
  2. 增加字段宽度。
  3. 改为字段组说明。
- 禁止在高密度双列表单中让 Label 多行换行后挤压整体节奏。

#### C. 可选项规则

- 若需要标识非必填，统一使用一种写法：
  - `Optional`
  - 或 `选填`
- 不允许同一系统里混用 `Optional / 选填 / 非必填`。

### 8.5 Control Row 统一骨架规范

Control Row 是所有字段最容易“炸”的地方，必须统一骨架职责。

#### A. 通用骨架

所有单行字段类控件统一使用：

```txt
Control Row
├─ Prefix Slot（可选）
├─ Value Slot（必须）
└─ Suffix Slot（可选）
```

#### B. Control Row 尺寸

##### 表单区

- 高度：`32px`
- 左右内边距：`10px`
- 圆角：`10px`
- 边框：`1px solid #E6E7EC`

##### Toolbar 区

- 高度：`36px`
- 左右内边距：`10px`
- 圆角：`10px`
- 边框：`1px solid #E6E7EC`

#### C. 内部布局

统一要求：

- `display: flex`
- `align-items: center`
- `min-width: 0`
- `gap: 8px`

槽位职责：

##### Prefix Slot

- `flex: 0 0 auto`
- 不参与压缩。
- 图标常用尺寸：`14px` 或 `16px`。

##### Value Slot

- `flex: 1 1 auto`
- `min-width: 0`
- 承载输入值、已选值、placeholder。
- 是唯一允许压缩的区域。

##### Suffix Slot

- `flex: 0 0 auto`
- 不参与压缩。
- 用于清空按钮、下拉箭头、状态图标、计数器等。

### 8.6 单值字段防换行规范

本节是新增重点，用于解决字段内容在控件内部换行的问题。

适用于：

- Input
- Search
- Select
- 单选 Dropdown
- Date / Month / Time 单值选择器
- 所有“控件内只展示一个值”的字段

#### A. 强制规则

单值字段中的值文本：

- 必须单行显示。
- 不得自动换行。
- 不得把一个语义值拆成两行。
- 不得因为箭头图标或清空按钮存在而被挤成两行。

必须设置：

```css
white-space: nowrap;
overflow: hidden;
text-overflow: ellipsis;
min-width: 0;
```

#### B. 明确禁止的情况

以下情况全部禁止：

- `2025年3月` 被拆成两行。
- `北京市朝阳区` 被拆成两行。
- 选中值第一行，Chevron 第二行。
- 图标在上、文本在下。
- Placeholder 换成两行。
- 清空按钮把文本挤成两行。

#### C. 宽度不够时的处理优先级

当字段宽度不足时，处理优先级必须为：

1. 优先扩宽字段。
2. 其次让字段整体换到下一行。
3. 其次将低优先级字段收进“更多筛选”。
4. 最后才允许文本省略。
5. 绝不允许控件内部值文本换行。

#### D. 单值字段默认文本对齐

- 表单字段中的值默认左对齐。
- 列表筛选区字段中的值默认左对齐。
- Select / Dropdown / Month Select 不建议居中展示值文本。
- 只有设计稿明确要求“按钮式选择器”时，才允许居中。

补充说明：

- 值文本被放在居中容器中又没有 `nowrap` 时，在可用空间不足的情况下非常容易上下断开，必须避免。

### 8.7 语义最小宽度规范

表单字段不能只写一个统一的 `min-width: 100px`，因为不同字段的语义内容长度不同，必须增加“语义最小宽度”。

#### A. 单行字段最小宽度表

| 字段类型                       | 最小宽度 | 推荐宽度 | 说明                    |
| ------------------------------ | -------- | -------- | ----------------------- |
| 极短枚举字段（如 性别、是/否） | `100px`  | `120px`  | 可压缩但不建议更小      |
| 普通 Input                     | `100px`  | `160px`  | 根据表单容器自适应      |
| 普通 Select / Dropdown         | `120px`  | `160px`  | 防止值与箭头互挤        |
| 年 / 月 / 年月选择器           | `112px`  | `120px`  | 如 `2025年3月` 不得换行 |
| 日期选择器                     | `120px`  | `140px`  | 如 `2025-03-30`         |
| 时间选择器                     | `100px`  | `120px`  | 如 `12:30`              |
| 日期时间选择器                 | `160px`  | `180px`  | 信息量更长              |
| 组织 / 校区 / 班级等名称选择器 | `140px`  | `180px`  | 建议允许省略            |
| Search                         | `120px`  | `200px`  | 真实列表页建议更宽      |

#### B. 年月选择器专项规则

像 `2025年3月` 类型控件，统一要求：

- 表单区最小宽度：`112px`
- 推荐宽度：`120px`
- 值文本强制单行。
- 值文本左对齐。
- 右侧 Chevron 独立占位，不参与压缩。
- 宽度不足时允许整体变窄后省略，但不允许换行。

推荐宽度计算方式：

`左 padding 10 + 文本安全宽度 56~60 + 图标间距 8 + Chevron 14 + 右 padding 10 ≈ 98~102px`

考虑字体渲染、缩放、不同月份字符宽度波动，最终工程最小宽度建议抬到 `112px`，推荐 `120px`。

### 8.8 文本与图标的内部约束

#### A. 文本规范

单行字段文本统一：

- 值文本：`14px / 22px / 400 / #01113D`
- Placeholder：`14px / 22px / 400 / #8C94A7`
- Disabled：`14px / 22px / 400 / #BFC3CE`

#### B. 图标规范

| 位置                      | 尺寸             | 与文本间距 |
| ------------------------- | ---------------- | ---------- |
| 搜索图标                  | `16px`           | `8px`      |
| Select / Dropdown Chevron | `14px`           | `8px`      |
| 清空按钮图标              | `14px` 或 `16px` | `8px`      |
| 状态图标                  | `14px`           | `8px`      |

#### C. 压缩规则

- 图标区永远不压缩。
- 清空按钮永远不压缩。
- Chevron 永远不压缩。
- 文本区是唯一允许压缩的区域。
- 文本区压缩后必须省略，不得换行。

### 8.9 Message Row 规范

#### A. 尺寸

- 最小高度：`20px`
- 上间距：`4px`

#### B. 文本规范

| 类型    | 字号 / 行高 / 字重 / 颜色     |
| ------- | ----------------------------- |
| Helper  | `12px / 20px / 400 / #5A6481` |
| Error   | `12px / 20px / 400 / #EF4437` |
| Warning | `12px / 20px / 400 / #EA580C` |
| Success | `12px / 20px / 400 / #16A34A` |

#### C. 显示规则

- 同一时刻只显示一个主反馈。
- Error 优先级最高。
- 有 Error 时不再显示 Helper。
- 高密度表单建议预留 Message Row，以避免错误出现时整体跳动。

#### D. 换行规则

- Message Row 默认单行。
- 最多允许 `2` 行。
- 超过 `2` 行时，应优先：
  1. 缩短文案。
  2. 改为字段组级说明。
  3. 改为表单级错误汇总。

禁止：

- 在双列表单中让某个字段的错误文案无限换行，把整行高度撑得与其他字段严重不一致。

### 8.10 表单布局规范

#### A. 单列表单

- 字段纵向间距：`16px`
- 字段宽度：`100%`
- 推荐用于：
  - 复杂字段
  - 文案较长字段
  - Textarea
  - 上传类字段
  - 详情编辑页

#### B. 双列表单

- 列间距：`12px`
- 行间距：`16px`
- 每列字段默认平分宽度。

计算方式：

```txt
字段宽度 = (容器宽度 - 12px) / 2
```

#### C. 双列降级规则

若任一列实际宽度小于 `220px`，必须降级为单列。

原因：

- 小于 `220px` 时，字段内容、Label、错误反馈容易开始互相挤压。
- Select / Dropdown / 日期字段更容易出现值省略过度或换行风险。

#### D. 弹窗表单建议

对于 `560px` 宽弹窗：

- Body 左右 padding：`24px`
- 内部可用宽度：`512px`
- 双列 gap：`12px`
- 单列宽度约：`250px`

因此：

- `560px` 宽弹窗适合做双列。
- 但如果字段普遍较长、错误提示较多、含日期时间与复杂下拉，建议使用单列。

### 8.11 字段组规范

#### A. 字段组间距

- 字段组标题与字段列表间距：`8px`
- 字段组与字段组之间：`24px`

#### B. 字段组标题

- `14px / 22px / 500 / #27345A`
- 不建议比普通 Label 更重。
- 字段组标题不替代具体字段 Label。

### 8.12 状态与只读 / 禁用 / 加载规范

#### A. 字段状态优先级

`disabled` > `readonly` > `error` > `focus / open` > `filled` > `hover` > `default`

#### B. `readonly` 与 `disabled` 区别

##### `readonly`

- 可读
- 可选中文本
- 不可编辑
- 不显示清空按钮
- 不强化 hover
- 不应显著变灰

##### `disabled`

- 不可编辑
- 不可聚焦
- 不可触发
- 明显弱化
- 必须同步原生语义

#### C. `loading`

- Loading 中不得重复提交。
- Loading 中若字段仍可输入，必须有明确产品理由。
- 默认字段 loading 应阻断值切换。

### 8.13 清空按钮与后缀区规范

#### A. 清空按钮出现条件

- 有值
- 可编辑
- 非 `readonly`
- 非 `disabled`

#### B. 清空按钮尺寸

- 热区不小于：`20px x 20px`
- 图标：`14px` 或 `16px`

#### C. 多后缀共存规则

当一个字段同时存在：

- 清空按钮
- 状态图标
- Chevron

必须遵循：

- 后缀区整体 `flex: 0 0 auto`
- 图标之间间距：`8px`
- 不允许后缀图标压缩文本区到换行。

### 8.14 字段值截断规则

#### A. 单值字段

- 必须单行省略。
- 默认使用 `text-overflow: ellipsis`。

#### B. 多值字段（多选 Dropdown）

- 已选项优先使用 Chip。
- 默认最多直出前 `2` 项。
- 超出后折叠为 `+n`。
- 不允许把 Chip 挤成两层导致控件高度失控，除非该字段明确设计为自增高模式。

#### C. Textarea

- Textarea 是唯一默认允许多行的字段。
- 不继承单值字段防换行规则。

### 8.15 键盘与可访问性规范

- Label 必须与 Control 建立明确语义关联。
- Error 文案必须可被读屏感知。
- 必填语义不能只依赖红色星号。
- Select / Dropdown / Date 类单值字段必须支持键盘打开、选择、关闭。
- 单值字段中的 Chevron 只是提示图标，不单独承担唯一交互入口。
- 图标按钮型后缀必须具备可读名称。

### 8.16 推荐实现约束

#### A. 单行字段基础样式

```css
.field {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.field__label {
  min-height: 22px;
  font-size: 14px;
  line-height: 22px;
  font-weight: 500;
  color: #27345a;
}

.field__control {
  margin-top: 4px;
  height: 32px;
  padding: 0 10px;
  border: 1px solid #e6e7ec;
  border-radius: 10px;
  background: #ffffff;
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 0;
}

.field__prefix,
.field__suffix {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
}

.field__value {
  flex: 1 1 auto;
  min-width: 0;
  font-size: 14px;
  line-height: 22px;
  font-weight: 400;
  color: #01113d;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.field__message {
  margin-top: 4px;
  min-height: 20px;
  font-size: 12px;
  line-height: 20px;
}
```

#### B. Toolbar 版本

```css
.field--toolbar .field__control {
  margin-top: 0;
  height: 36px;
}
```

#### C. 年月选择器专项约束

```css
.field--month {
  min-width: 112px;
}

.field--month .field__value {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

### 8.17 明确禁止项

以下情况在全系统中禁止：

1. 单值字段值文本换成两行。
2. 值文本和 Chevron 上下错层。
3. 清空按钮把值文本挤成两行。
4. Select 触发器内部文本居中后自然换行。
5. 双列字段宽度小到内容无法稳定展示却仍强行双列。
6. 错误文案出现时整页表单跳动严重。
7. `readonly` 做成和 `disabled` 一样灰。
8. 表单区和 Toolbar 区混用 `32px` / `36px` 高度。
9. 字段内图标与文本没有固定 gap。
10. 用浏览器默认换行来“自适应”字段内容。

### 8.18 验收清单

#### A. 结构验收

- 是否所有字段都遵循 `Label / Control / Message` 三层结构。
- 是否同一表单区字段占高一致。
- 是否存在错误文案出现后布局大跳动。

#### B. 视觉验收

- Label、Value、Message 字阶是否统一。
- Control 高度、圆角、边框、padding 是否统一。
- 图标尺寸与间距是否统一。

#### C. 稳定性验收

- 单值字段在窄宽度下是否仍保持单行。
- `2025年3月`、较长状态文案、组织名等是否不会换行。
- 清空按钮、Chevron、状态图标共存时是否仍不炸布局。
- 双列宽度不足时是否会自动降级，而不是继续挤压。

#### D. 交互验收

- `readonly` 与 `disabled` 是否表现不同。
- `error / focus / open / loading` 是否优先级正确。
- 键盘是否可聚焦、操作、关闭。

---

## 9. 弹窗系统规范

### 9.1 弹窗类型

| 类型           | 尺寸            | 说明                   |
| -------------- | --------------- | ---------------------- |
| 流程型表单弹窗 | `560px x 589px` | 导入、编辑、配置类弹窗 |
| 结果反馈弹窗   | `520px x 370px` | 成功 / 失败结果反馈    |
| 提示弹窗       | `450px x 294px` | 说明、提醒             |
| 危险确认弹窗   | `450px x 248px` | 删除、作废、停用确认   |

### 9.2 Overlay 规范

- 背景色：`rgba(0, 0, 0, 0.5)`
- 覆盖范围：全屏
- 必须阻断页面滚动
- 默认拦截背景点击
- 是否支持 `Esc` 关闭由业务决定

### 9.3 Surface 规范

- 背景色：`#FFFFFF`
- 圆角：`24px`
- 边框：`1px solid #E6E7EC`
- 阴影：`0 20px 40px rgba(0, 0, 0, 0.12), 0 8px 16px rgba(0, 0, 0, 0.08)`
- `overflow: hidden`

### 9.4 Header 规范

- 高度：`52px`
- 左右内边距：`16px`
- 底部分割线：`1px solid #E6E7EC`
- 标题字号：`16px`
- 标题行高：`24px`
- 标题字重：`500`
- 标题颜色：`#01113D`
- 关闭按钮热区：`32px x 32px`
- 关闭图标尺寸：`20px x 20px`

### 9.5 Body 规范

- 主内边距：`24px`
- 模块间距：`16px`
- 内容区默认使用 `flex-direction: column`
- 长内容允许滚动，但只允许 Body 滚动
- 普通正文：`14px / 22px / 400 / #01113D`
- 辅助说明：`14px / 22px / 400 / #5A6481`

### 9.6 Footer 规范

- 高度：`52px`
- 左右内边距：`16px`
- 顶部分割线：`1px solid #E6E7EC`
- 按钮间距：`12px`
- 对齐方式：靠右
- 顺序：次按钮在左，主按钮在右

### 9.7 表单型弹窗内部字段规范

- 双列表单项列间距：`12px`
- Label 与控件间距：`4px`
- Input / Select 高度：`32px`
- Dropdown 高度：如为表单型选择控件，优先与 Select 保持一致为 `32px`；若为独立展示型多选下拉，可按组件章节单独定义
- Textarea 高度：`68px`
- 步骤条高度：`40px`
- 步骤条左右内边距：`12px`
- 步骤条背景：`#F7F8F9`
- 步骤条标题：`14px / 22px / 500 / #27345A`

### 9.8 结果型弹窗规范

- 无常规 Header
- 右上角悬浮关闭按钮
- 状态图区域尺寸：`90px x 90px`
- 标题：`22px / 30px / 600 / #01113D`
- 正文：`14px / 22px / 400 / #5A6481`

### 9.9 危险确认弹窗规范

- 风险图标颜色：`#EF4437`
- 风险主按钮背景：`#EF4437`
- Hover：`#DC2626`
- 标题：`16px / 24px / 500 / #01113D`
- 正文：`14px / 22px / 400 / #5A6481`
- 明确告知“删除后无法恢复”或等价风险

---

## 10. Button 规范

### 10.1 适用范围

- 页内操作按钮
- 弹窗 Footer 按钮
- 工具栏操作按钮
- 下拉触发按钮
- 内联操作按钮

### 10.2 支持变体

- `primary`
- `secondary`
- `tertiary`
- `quaternary`
- `ghost`

### 10.3 尺寸规范

| 尺寸 | 高度   | 左右 padding | 圆角   | 字号   | 行高   | 字重  |
| ---- | ------ | ------------ | ------ | ------ | ------ | ----- |
| `sm` | `28px` | `12px`       | `10px` | `12px` | `18px` | `500` |
| `md` | `36px` | `14px`       | `10px` | `14px` | `20px` | `500` |

### 10.4 宽度与自适应规则

- Button 默认不设置固定宽度，宽度必须由内容字数、图标数量和左右 padding 共同决定。
- 推荐使用 `width: auto`，禁止为普通文字按钮统一写死固定宽度。
- Button 最终宽度计算原则：
  - 左右 padding 固定
  - 中间文字按实际字数自然撑开
  - 有图标时再叠加图标宽度与图标间距
- 单行按钮必须保持 `white-space: nowrap`，避免因为字数变化出现按钮内换行。
- 普通操作按钮应保证最小可点击宽度：
  - `sm` 最小宽度建议 `56px`
  - `md` 最小宽度建议 `72px`
- 仅以下场景允许使用固定宽度按钮：
  - 弹窗 Footer 中设计稿明确要求等宽按钮
  - 业务页面中的成组操作按钮明确要求视觉对齐
  - 图标按钮 `icon-only`
- 若同一组按钮需要视觉对齐，应由外层布局统一控制宽度，不应改写 Button 基础组件的默认自适应策略。

### 10.5 各变体视觉规范

| 变体         | 默认背景  | 默认边框  | 默认文字  | Hover          | Active         | Disabled                       |
| ------------ | --------- | --------- | --------- | -------------- | -------------- | ------------------------------ |
| `primary`    | `#0445fc` | `#0445fc` | `#FFFFFF` | 背景 `#005BC4` | 背景 `#004493` | 背景 `#99C7FB`，文字 `#FFFFFF` |
| `secondary`  | `#F7F8F9` | `#E6E7EC` | `#01113D` | 背景 `#EDEEF1` | 背景 `#D9DCE2` | 背景 `#F2F3F5`，文字 `#BFC3CE` |
| `tertiary`   | `#E6F1FE` | `#E6F1FE` | `#0445fc` | 背景 `#D7EAFE` | 背景 `#C7E1FE` | 背景 `#EAF4FE`，文字 `#99C7FB` |
| `quaternary` | `#E6F1FE` | `#0445fc` | `#0445fc` | 背景 `#DCEBFE` | 背景 `#D1E5FE` | 边框 `#99C7FB`，文字 `#99C7FB` |
| `ghost`      | `#FFFFFF` | `#E6E7EC` | `#01113D` | 背景 `#F7F8F9` | 背景 `#EDEEF1` | 背景 `#FFFFFF`，文字 `#BFC3CE` |

### 10.6 图标规范

- 左图标尺寸：`14px`
- 纯图标按钮：
  - `sm`：`28px x 28px`
  - `md`：`36px x 36px`
- 图标与文字间距：`6px`

### 10.7 文字与留白控制规则

- 按钮宽度必须跟随文案长度变化，不允许出现“2 个字和 8 个字宽度完全一样”的默认实现，除非设计稿明确要求等宽。
- 中文按钮文案建议控制在 `2` 到 `8` 个字内，超长文案应优先通过文案优化解决，而不是无限拉宽按钮。
- 当按钮文案较短时，仍需保留左右 padding，禁止把按钮压缩成紧贴文字的形态。
- 当按钮文案较长时，优先增加按钮宽度，不允许压缩左右 padding 到低于规范值。
- 带左图标按钮宽度计算规则：
  - 左 padding + 图标宽度 + 图标与文字间距 + 文字宽度 + 右 padding
- 纯图标按钮宽度不跟随文字规则，严格等于按钮高度。

### 10.8 开发要求

- 所有按钮必须具备点击反馈。
- `disabled` 必须使用原生 `disabled`。
- 危险按钮应基于 `primary` 扩展，不单独发明新按钮体系。

---

## 11. Search 规范

### 11.1 基础尺寸

| 场景                  | 宽度    | 最小宽度 | 高度   | 圆角   | 左右 padding |
| --------------------- | ------- | -------- | ------ | ------ | ------------ |
| 组件库状态展示        | `280px` | `280px`  | `36px` | `10px` | `10px`       |
| 列表页 Toolbar 筛选区 | `auto`  | `100px`  | `36px` | `10px` | `10px`       |

### 11.2 结构规范

- 左侧固定搜索图标
- 图标尺寸：`16px x 16px`
- 图标与文字间距：`8px`
- Filled 状态右侧显示清空按钮
- 清空按钮热区建议：`20px x 20px`

### 11.3 视觉规范

| 状态       | 背景      | 边框      | 文字      | 图标      |
| ---------- | --------- | --------- | --------- | --------- |
| `default`  | `#FFFFFF` | `#E6E7EC` | `#01113D` | `#8C94A7` |
| `hover`    | `#FFFFFF` | `#CFCFD6` | `#01113D` | `#5A6481` |
| `focus`    | `#FFFFFF` | `#0445fc` | `#01113D` | `#0445fc` |
| `filled`   | `#FFFFFF` | `#E6E7EC` | `#01113D` | `#5A6481` |
| `error`    | `#FFFFFF` | `#EF4437` | `#01113D` | `#EF4437` |
| `disabled` | `#F2F3F5` | `#E6E7EC` | `#BFC3CE` | `#BFC3CE` |

### 11.4 文字规范

- 输入内容：`14px / 22px / 400 / #01113D`
- Placeholder：`14px / 22px / 400 / #8C94A7`
- Disabled：`14px / 22px / 400 / #BFC3CE`

### 11.5 列表页自适应规则

- 列表页 Toolbar 中，Search 不得写死 `280px`。
- 必须支持：
  - `min-width: 100px`
  - `width: auto`
  - `flex: 1 1 auto`
- 宽度应由 Toolbar 容器统一控制，组件内部不应写死固定宽度。
- 筛选项应尽量保持在同一行，空间不足时才允许换行。

---

## 12. Input 规范

### 12.1 基础尺寸

| 场景                  | 宽度    | 最小宽度 | 高度   | 圆角   | 左右 padding |
| --------------------- | ------- | -------- | ------ | ------ | ------------ |
| 组件库状态展示        | `280px` | `280px`  | `36px` | `10px` | `10px`       |
| 表单页面 / 表单区     | `auto`  | `100px`  | `32px` | `10px` | `10px`       |
| 列表页 Toolbar 筛选区 | `auto`  | `100px`  | `36px` | `10px` | `10px`       |

补充规则：

- 表单页面、抽屉表单、弹窗表单、详情编辑区中的 Input，默认使用 `32px` 高度。
- 列表页顶部筛选区中的 Input，仍使用 `36px` 高度。
- 若同一页面同时存在“表单区”和“列表筛选区”，两者不得混用同一高度。

### 12.2 状态规范

| 状态       | 背景      | 边框      | 文字      | Placeholder |
| ---------- | --------- | --------- | --------- | ----------- |
| `default`  | `#FFFFFF` | `#E6E7EC` | `#01113D` | `#8C94A7`   |
| `hover`    | `#FFFFFF` | `#CFCFD6` | `#01113D` | `#8C94A7`   |
| `focus`    | `#FFFFFF` | `#0445fc` | `#01113D` | `#8C94A7`   |
| `filled`   | `#FFFFFF` | `#E6E7EC` | `#01113D` | `#8C94A7`   |
| `error`    | `#FFFFFF` | `#EF4437` | `#01113D` | `#8C94A7`   |
| `disabled` | `#F2F3F5` | `#E6E7EC` | `#BFC3CE` | `#BFC3CE`   |

### 12.3 结构规则

- 有 Label 时采用纵向布局。
- Label 与控件间距：`4px`
- Filled / Error 且可编辑时可显示清空按钮。
- 清空按钮热区建议：`20px x 20px`

### 12.4 文字规范

- Label：`14px / 22px / 500 / #27345A`
- 输入内容：`14px / 22px / 400 / #01113D`
- Placeholder：`14px / 22px / 400 / #8C94A7`
- Error：`12px / 20px / 400 / #EF4437`
- Helper：`12px / 20px / 400 / #5A6481`

### 12.5 列表页自适应规则

- 列表筛选区中的 Input 不得固定 `280px` 或 `256px`。
- 必须支持：
  - `min-width: 100px`
  - `width: auto`
  - `flex: 1 1 auto`
- 若筛选条件较多，应优先压缩宽度而不是强制换行。

---

## 13. Select 规范

### 13.1 基础尺寸

| 场景                  | 宽度    | 最小宽度 | 高度   | 圆角   | 左右 padding |
| --------------------- | ------- | -------- | ------ | ------ | ------------ |
| 组件库状态展示        | `256px` | `256px`  | `36px` | `10px` | `10px`       |
| 表单页面 / 表单区     | `auto`  | `100px`  | `32px` | `10px` | `10px`       |
| 列表页 Toolbar 筛选区 | `auto`  | `100px`  | `36px` | `10px` | `10px`       |

补充规则：

- 表单页面、弹窗表单、抽屉表单中的 Select 触发器高度默认使用 `32px`。
- 列表页 Toolbar 筛选区中的 Select 触发器高度仍使用 `36px`。
- 同一表单区内的 Input 与 Select 必须保持同高，禁止一个 `32px`、一个 `36px` 混用。

### 13.2 关闭态规范

- 背景：`#FFFFFF`
- 边框：`1px solid #E6E7EC`
- 圆角：`10px`
- 左侧显示值或占位
- 右侧固定 Chevron
- 箭头尺寸：`14px x 14px`

### 13.3 展开态规范

- 边框切换为 `#0445fc`
- 箭头可翻转
- Dropdown Panel 宽度默认与触发器等宽
- Panel 背景：`#FFFFFF`
- Panel 边框：`1px solid #E6E7EC`
- Panel 圆角：`14px`
- Panel 阴影：三级浮层阴影
- 选项最小高度：`36px`
- 选项左右内边距：`12px`
- 选项 Hover 背景：`#E6F1FE`
- 选中项文字：`#0445fc`
- 选中项可带勾选图标

触发器高度补充：

- 表单页面 / 表单区：`32px`
- 列表页 Toolbar 筛选区：`36px`

### 13.4 文字规范

- Label：`14px / 22px / 500 / #27345A`
- 已选值：`14px / 22px / 400 / #01113D`
- Placeholder：`14px / 22px / 400 / #8C94A7`
- 选项文字：`14px / 22px / 400 / #01113D`
- 空态文案：`14px / 22px / 400 / #5A6481`

### 13.5 列表页自适应规则

- 列表页筛选区中的 Select 不允许固定 `256px`。
- 必须支持：
  - `min-width: 100px`
  - `width: auto`
  - `flex: 1 1 auto`
- 宽度应根据筛选条件数量自动分配，尽量保证同一行展示。
- 固定宽度 `256px` 仅用于组件库状态展示页、设计状态矩阵页。

---

## 14. Dropdown 规范

### 14.1 组件分类

- 单选 Dropdown
- 多选 Dropdown

### 14.2 基础尺寸

| 场景                  | 宽度    | 最小宽度 | 最小高度 | 圆角   | 左右 padding |
| --------------------- | ------- | -------- | -------- | ------ | ------------ |
| 组件库状态展示        | `256px` | `256px`  | `36px`   | `10px` | `10px`       |
| 列表页 Toolbar 筛选区 | `auto`  | `100px`  | `36px`   | `10px` | `10px`       |

### 14.3 单选 Dropdown 规范

- 点击触发器才展开菜单，默认不常驻展开。
- 点击后立即进入 Focus 态。
- 已选值显示在输入区域中。
- 触发器默认高度：`36px`
- 文字垂直居中
- 右侧保留箭头图标

### 14.4 多选 Dropdown 规范

- 已选项通过 Tag / Chip 展示
- Chip 高度建议：`22px`
- Chip 左右内边距：`8px`
- Chip 间距：`6px`
- 默认最多展示前 2 项
- 超过 2 项后折叠为 `+n`
- 支持清空全部已选项
- 右侧支持清空按钮和箭头共存

### 14.5 菜单规范

- 宽度默认与触发器一致
- 背景：`#FFFFFF`
- 边框：`1px solid #E6E7EC`
- 圆角：`14px`
- 阴影：三级浮层阴影
- 面板内边距：`6px`
- 选项最小高度：`36px`
- 选项左右内边距：`12px`
- 选项圆角：`10px`
- Hover 背景：`#E6F1FE`
- Selected 文本色：`#0445fc`
- Selected 右侧勾选图标尺寸：`14px`
- 空态文案：`No results found`

### 14.6 文字规范

- 触发器内容：`14px / 22px / 400 / #01113D`
- Placeholder：`14px / 22px / 400 / #8C94A7`
- 选项文字：`14px / 22px / 400 / #01113D`
- 空态文案：`14px / 22px / 400 / #5A6481`
- Chip 文案：`12px / 20px / 400`

### 14.7 列表页自适应规则

- 列表页筛选区中的 Dropdown 不允许固定 `256px`。
- 建议使用：
  - `min-width: 100px`
  - `width: auto`
  - `flex: 1 1 auto`
- 当筛选项数量较多时，应优先保持一行布局，容器不足时才换行。

---

## 15. Textarea 规范

### 15.1 基础尺寸

- 宽度：组件库展示建议 `280px`
- 高度：`68px`
- 左右 padding：`10px`
- 上下 padding：`8px`
- 圆角：`10px`

### 15.2 状态规范

| 状态       | 背景      | 边框      | 内容      | 计数器    |
| ---------- | --------- | --------- | --------- | --------- |
| `default`  | `#FFFFFF` | `#E6E7EC` | `#01113D` | `#5A6481` |
| `hover`    | `#FFFFFF` | `#CFCFD6` | `#01113D` | `#5A6481` |
| `focus`    | `#FFFFFF` | `#0445fc` | `#01113D` | `#5A6481` |
| `filled`   | `#FFFFFF` | `#E6E7EC` | `#01113D` | `#5A6481` |
| `error`    | `#FFFFFF` | `#EF4437` | `#01113D` | `#EF4437` |
| `disabled` | `#F2F3F5` | `#E6E7EC` | `#BFC3CE` | `#BFC3CE` |

### 15.3 固定规则

- 必须始终显示字符计数器。
- 计数器位置：右下角。
- 计数器与底边、右边建议间距：`8px`
- 默认 Placeholder：`请输入...`
- 超出 `maxLength` 时自动进入 Error 态。
- 右下角可保留轻量 resize indicator 视觉符号。

### 15.4 文字规范

- Label：`14px / 22px / 500 / #27345A`
- 内容：`14px / 22px / 400 / #01113D`
- Placeholder：`14px / 22px / 400 / #8C94A7`
- 计数器：`12px / 20px / 400 / #5A6481`
- Error 文案：`12px / 20px / 400 / #EF4437`

---

## 16. Tabs 规范

### 16.1 类型

- Primary Tabs
- Secondary Tabs

### 16.2 Primary Tabs 规范

- 形态：底部激活线
- 非激活文字颜色：`#5A6481`
- 激活文字颜色：`#0445fc`
- 激活线高度：`2px`
- 激活线颜色：`#0445fc`
- 标签间距：`24px`

| 尺寸 | 高度   | 字号   | 行高   | 字重  |
| ---- | ------ | ------ | ------ | ----- |
| `md` | `36px` | `14px` | `22px` | `500` |
| `lg` | `40px` | `16px` | `24px` | `500` |

### 16.3 Secondary Tabs 规范

- 容器背景：`#F7F8F9`
- 容器圆角：`10px`
- 容器内边距：`4px`
- 激活项背景：`#FFFFFF`
- 激活项阴影：`0 2px 6px rgba(1, 17, 61, 0.08)`
- 激活项文字：`#0445fc`
- 非激活项文字：`#5A6481`

| 尺寸 | 高度   | 横向 padding | 字号   | 行高   | 字重  |
| ---- | ------ | ------------ | ------ | ------ | ----- |
| `md` | `32px` | `12px`       | `14px` | `22px` | `500` |
| `lg` | `36px` | `14px`       | `16px` | `24px` | `500` |

### 16.4 禁用规范

- Disabled 文字：`#BFC3CE`
- Disabled 背景：保持原样但整体弱化
- 不可响应点击

---

## 17. Tag 规范

### 17.1 三种优先级

- `primary`：浅底色块 + 同色文字
- `secondary`：透明底 + 前置圆点 + 同色文字
- `tertiary`：纯色底 + 白字

### 17.2 尺寸规范

- 高度：`22px`
- 左右 padding：`8px`
- 圆角：`6px`
- 圆点尺寸：`4px`
- 文本字号：`12px`
- 文本行高：`20px`
- 文本字重：`400`

### 17.3 宽度与自适应规则

- Tag 默认不设置固定宽度，宽度必须由文本字数、图标/圆点和左右 padding 自适应决定。
- 推荐使用 `width: auto` 或 `inline-flex`，禁止将所有 Tag 写成统一固定宽度。
- Tag 必须保持单行展示，建议设置 `white-space: nowrap`。
- 短文案 Tag 不得因内容少而丢失左右留白。
- 长文案 Tag 应优先自然增宽；若业务场景必须限制宽度，应通过外层容器控制，并配合省略处理。
- `secondary` 类型包含前置圆点时，宽度计算规则为：
  - 左 padding + 圆点宽度 + 圆点与文字间距 + 文字宽度 + 右 padding
- `primary / tertiary` 类型宽度计算规则为：
  - 左 padding + 文字宽度 + 右 padding
- Tag 最小宽度建议为 `fit-content`，不单独规定统一像素值。

### 17.4 颜色建议

| Tone    | Primary 背景 | Primary 文字 | Tertiary 背景 | Tertiary 文字 |
| ------- | ------------ | ------------ | ------------- | ------------- |
| brand   | `#E6F1FE`    | `#0445fc`    | `#0445fc`     | `#FFFFFF`     |
| success | `#EBF9F0`    | `#16A34A`    | `#16A34A`     | `#FFFFFF`     |
| danger  | `#FDECEC`    | `#EF4437`    | `#EF4437`     | `#FFFFFF`     |
| warning | `#FFF7ED`    | `#EA580C`    | `#EA580C`     | `#FFFFFF`     |
| info    | `#E8F7FB`    | `#0891B2`    | `#0891B2`     | `#FFFFFF`     |
| neutral | `#F7F8F9`    | `#5A6481`    | `#5A6481`     | `#FFFFFF`     |

### 17.5 文字与留白控制规则

- Tag 左右 padding 必须始终保留 `8px`，不得因字数少而压缩。
- 单字、双字、三字状态标签都应通过自适应宽度自然变化，不能被强行拉伸为统一宽度。
- 若 Tag 内加入关闭图标或状态图标，应在原有自适应宽度基础上额外增加图标宽度与间距。
- 同一行多个 Tag 排列时，组件本身仍保持内容自适应，间距由外层布局控制，不通过给 Tag 固定宽度实现对齐。

### 17.6 文字规范

- 统一使用：`12px / 20px / 400`
- `primary / secondary` 文字颜色跟随 tone
- `tertiary` 统一白字

---

## 18. Alert 规范

Alert 用于表单区、弹窗内、页面内容区和表格上方的轻量反馈提示，承载说明、成功、失败、错误统计等短反馈信息。Alert 不是弹窗，不抢占页面焦点，不阻断主流程。

### 18.1 适用场景

- 表单填写说明
- 上传结果反馈
- 导入成功提示
- 导入失败统计
- 可恢复错误说明
- 带操作入口的轻提示

### 18.2 状态分类

| 状态      | 语义                    | 典型文案                                          |
| --------- | ----------------------- | ------------------------------------------------- |
| `explain` | 普通说明 / 中性说明     | `文字说明`                                        |
| `danger`  | 明确失败 / 风险提醒     | `上传失败！`                                      |
| `success` | 成功反馈                | `导入成功！`                                      |
| `error`   | 错误统计 / 需进一步处理 | `共导入1个，成功0个，失败2个，请修改后再重新导入` |

### 18.3 容器规范

| 属性           | 规则                                                                  |
| -------------- | --------------------------------------------------------------------- |
| 高度           | 自适应高度，单行内容最小高度建议 `42px`                               |
| 组件库展示宽度 | `506px`                                                               |
| 业务页面宽度   | 默认跟随父容器，建议 `width: 100%`                                    |
| 最小宽度       | `160px`                                                               |
| 内边距         | `12px`                                                                |
| 圆角           | `14px`                                                                |
| 边框           | `1px solid`                                                           |
| 布局           | `display: flex; align-items: center; justify-content: space-between;` |
| 内容间距       | 左侧图标与文案间距 `12px`                                             |
| 换行策略       | 优先单行；文案过长时允许换行，但操作链接应尽量保持在右侧              |

### 18.4 四种状态视觉规范

| 状态      | 背景色    | 边框色    | 主图标色  | 主文案色  |
| --------- | --------- | --------- | --------- | --------- |
| `explain` | `#F7F8F9` | `#E6E7EC` | `#01113D` | `#01113D` |
| `danger`  | `#FEF2F2` | `#FECACA` | `#FF6559` | `#01113D` |
| `success` | `#F0FDF4` | `#BBF7D0` | `#16A34A` | `#01113D` |
| `error`   | `#FFF7ED` | `#FED7AA` | `#EA580C` | `#01113D` |

补充规则：

- `error` 状态中的错误数量强调色使用 `#FF0A0A`。
- `error` 状态中的右侧操作链接颜色使用 `#004493`。
- Alert 默认不使用阴影。
- Alert 默认不使用关闭按钮，若业务需要关闭能力，应在设计明确时再扩展。

### 18.5 图标规范

| 状态      | 图标建议         | 尺寸          |
| --------- | ---------------- | ------------- |
| `explain` | `info`           | `18px x 18px` |
| `danger`  | `alert-circle`   | `18px x 18px` |
| `success` | `check-circle`   | `18px x 18px` |
| `error`   | `alert-triangle` | `18px x 18px` |

图标要求：

- 图标必须左侧对齐。
- 图标颜色必须跟随状态语义，不允许统一灰色。
- 图标使用内联 SVG，颜色优先使用 `currentColor`。
- 图标与正文垂直居中对齐。

### 18.6 文字规范

| 文本类型     | 字号   | 行高   | 字重  | 颜色      |
| ------------ | ------ | ------ | ----- | --------- |
| 主文案       | `13px` | `20px` | `400` | `#01113D` |
| 强调数字     | `13px` | `20px` | `400` | `#FF0A0A` |
| 右侧操作链接 | `13px` | `20px` | `400` | `#004493` |

补充规则：

- Alert 文字默认使用 `Alibaba PuHuiTi 3.0`。
- 单行说明文案应垂直居中。
- 含统计信息的长文案默认左对齐。
- 右侧操作链接默认不换行，建议设置 `white-space: nowrap`。

### 18.7 结构规范

#### A. 普通说明型 Alert

- 左侧：信息图标
- 右侧：单行说明文案
- 不带额外操作入口

#### B. 成功 / 失败型 Alert

- 左侧：状态图标
- 右侧：单行结果文案
- 文案保持简短直接

#### C. 错误统计型 Alert

- 左侧：警告图标
- 中间：错误统计文案
- 右侧：操作链接，如 `下载错误报告`
- 三段布局应保持一行优先展示

### 18.8 宽度与自适应规则

- Alert 默认不建议写死业务宽度，推荐：
  - `width: 100%`
  - `min-width: 160px`
- 组件库状态页可以保留 `506px` 作为设计对照宽度。
- 当 Alert 放在弹窗内、表单区、表格上方提示区时，应优先跟随容器宽度。
- 长文案 Alert 应优先扩展容器宽度；若外层空间不足，可允许正文换行，但不应压缩左右内边距。
- 带右侧链接的 Alert，右侧链接区域不应因为宽度不足被挤压到图标下方，必要时应整体换为两行布局。

### 18.9 交互规范

- 普通 Alert 默认不可点击。
- 带右侧操作文案时，仅操作文案可点击，不能让整个 Alert 容器误变成可点击区域。
- 操作链接 Hover 时建议：
  - 文字保持 `#004493`
  - 出现下划线
- Alert 不应在页面加载后自动消失，除非业务明确要求做 Toast 化处理。

### 18.10 开发要求

- Alert 应封装为独立基础组件，不应在每个业务页面手写一份提示条。
- 4 种状态必须共用同一套结构，通过 `state` 或 `variant` 切换视觉。
- 错误统计型中的数字高亮和右侧链接必须支持独立渲染，不要把整段文案写成不可拆分纯字符串。
- Alert 组件必须支持在页面流中直接使用，也必须支持放入弹窗 Body 中使用。

---

## 19. Table 规范

本章节在原有表格规范基础上进行完整展开，所有 B 端系统表格页面必须统一遵守。

对于 Figma 节点 `439:20862` 对应的完整表格页面，页面级布局、卡片分层、列宽、区域顺序可参考已合并的《表格页面规范》；但页面内部使用的 `Button / Search / Input / Select / Dropdown / Tabs / Textarea / Tag` 不得被页面规范中的旧数值覆盖，仍必须优先遵循本文 `10 - 17` 章的组件规范与对应 Figma 组件节点。

### 19.1 总体原则

1. 禁止使用任何形式的斑马纹，包括交替行背景色，所有数据行背景必须保持统一颜色 `#FFFFFF`。
2. 表头字体必须使用 `font-weight: 400`。
3. 表格内容默认必须左对齐，禁止内容居中对齐。仅操作列、图标列、Checkbox 列允许居中。
4. 表格 Hover 状态必须使用品牌淡蓝色 `#F2F6FF`。
5. 容器背景色为 `#FFFFFF`，边框为 `1px solid #E6E7EC`，圆角为 `11px`。
6. Table Container 必须设置 `overflow: hidden`。
7. 表头高度必须固定为 `40px`。
8. 表头背景色必须为 `#F7F8F9`。
9. 表头字体大小必须为 `14px`。
10. 表头字体权重必须为 `400`。
11. 表头文字颜色必须为 `#01113D`。
12. 表头文字必须左对齐。
13. 表头左右内边距必须为 `12px`。
14. 表头底部必须有 `1px solid #E6E7EC` 分隔线。
15. 数据行最小行高必须为 `36px`。
16. 数据行背景必须为 `#FFFFFF`。
17. 表格内容字体大小必须为 `14px`。
18. 表格内容字体权重必须为 `400`。
19. 表格文字颜色必须为 `#01113D`。
20. 数据单元格必须左对齐。
21. 每个单元格内边距必须为 `12px`。
22. 每行底部必须有 `1px solid #E6E7EC` 分隔线。
23. Hover 过渡必须使用 `transition: background-color 0.2s`。
24. 空状态文字必须水平居中显示，默认文本为 `-`。
25. 空状态文字颜色必须为 `#5A6481`。
26. 操作列默认操作链接颜色必须为 `#032E92`。
27. 操作链接 Hover 时必须出现下划线。
28. 操作链接 Hover 时颜色保持不变或略微加深。
29. 高风险操作链接必须使用 `#FF6559`。
30. 表格页面结构必须包含 `Toolbar`、`Table Container`、`Pagination` 三个部分。
31. Toolbar 高度必须为 `56px`。
32. 所有 B 端系统表格页面都必须统一遵守以上 Table 设计系统规范。
33. 若页面级布局规范与基础组件规范冲突，页面结构规则服从页面规范，组件本体规则服从基础组件规范。

### 19.2 Table Container 规范

- 背景：`#FFFFFF`
- 边框：`1px solid #E6E7EC`
- 圆角：`11px`
- `overflow: hidden`
- 不允许子元素溢出破坏圆角

### 19.3 Thead 规范

| 属性         | 值                  |
| ------------ | ------------------- |
| 高度         | `40px`              |
| 背景色       | `#F7F8F9`           |
| 字号         | `14px`              |
| 行高         | `20px`              |
| 字重         | `400`               |
| 颜色         | `#01113D`           |
| 左右 padding | `12px`              |
| 底部分割线   | `1px solid #E6E7EC` |
| 对齐方式     | 默认左对齐          |

### 19.4 Tbody 规范

| 属性           | 值                      |
| -------------- | ----------------------- |
| 行最小高度     | `36px`                  |
| 行背景         | `#FFFFFF`               |
| Hover 背景     | `#F2F6FF`               |
| 单元格 padding | `12px`                  |
| 字号           | `14px`                  |
| 行高           | `20px`                  |
| 字重           | `400`                   |
| 字色           | `#01113D`               |
| 底部分割线     | `1px solid #E6E7EC`     |
| 过渡           | `background-color 0.2s` |

### 19.5 对齐规范

- 默认文本列：左对齐
- 数字列：建议左对齐；如业务明确为金额/统计，可右对齐，但需整列统一
- Checkbox 列：允许居中
- 图标列：允许居中
- 操作列：允许居中或右对齐，但需保持全表一致

### 19.6 空状态规范

- 空状态文本默认显示 `-`
- 水平居中
- 字号：`14px`
- 行高：`20px`
- 字重：`400`
- 颜色：`#5A6481`

### 19.7 操作列规范

- 默认操作链接：
  - 字号：`14px`
  - 行高：`20px`
  - 字重：`400`
  - 颜色：`#032E92`
- Hover：
  - `text-decoration: underline`
  - 颜色保持不变或略深
- 高风险操作：
  - 颜色：`#FF6559`
  - Hover 时仍保留下划线

### 19.8 节点 `439:20862` 页面级布局补充规范

以下规则来自已合并的表格页面布局规范，用于约束完整列表页的页面分层和卡片结构。

#### A. 页面主内容区结构

- 页面主内容区从上到下固定分为三层：
  1. `Tabs Bar`
  2. `Toolbar Card`
  3. `Table Card`
- 三层之间的垂直间距统一为 `12px`
- 主内容推荐实现宽度：`1272px`
- 主内容区使用常规文档流布局，禁止使用 `position: absolute` 拼主结构

#### B. Tabs Bar 页面容器规范

- 高度：`52px`
- 背景：`#FFFFFF`
- 边框：`1px solid #E6E7EC`
- 圆角：可跟随外层页面容器，不强制单独设置大圆角
- 当页面顶部以 Tab 直接承担标题职责时，可不额外显示独立页面标题
- Tabs Bar 内部的 Tab 样式、字号、激活态、禁用态，必须继续遵循本文 Tabs 规范对应章节及 Figma 节点 `423:122`

#### C. Toolbar Card 页面容器规范

Toolbar Card 在表格页面中至少存在两种合法形态，必须根据页面场景和功能需求判断使用哪一种，而不是固定只用一种布局。

##### 模式 A：列表筛选型 Toolbar Card

- 适用场景：
  - 列表页
  - 线索页
  - 名单页
  - 需要大量筛选条件的表格页
  - 需要批量操作和工具操作同时存在的页面
- 尺寸参考：`1272px x 104px`
- 背景：`#FFFFFF`
- 边框：`1px solid #E6E7EC`
- 圆角：`16px`
- 内边距：`12px`
- 结构：上下两行
- 每行参考高度：`32px`
- 两行间距：`16px`

##### 模式 B：详情头部操作型 Toolbar Card

- 对应参考节点：`502:7797`
- 适用场景：
  - 某个具体对象的详情页
  - 数据集、测试、计划、任务等单对象页面
  - 页面顶部需要返回上一级
  - 页面顶部需要展示标题和摘要信息
  - 页面只需要少量筛选和若干主操作，不需要完整双行筛选区
- 尺寸参考：`1232px x 72px`
- 实际视觉高度可按 `71.5px` 还原，工程上建议统一落为 `72px`
- 背景：`#FFFFFF`
- 边框：`1px solid #E6E7EC`
- 圆角：`14px`
- 内边距：建议以视觉还原为准，约等于 `13px 14px`
- 结构：单行
- 对齐方式：左侧“返回 + 标题摘要”，右侧“局部筛选 + 操作按钮”
- 内容垂直居中
- 容器内所有元素应保持单行优先展示

##### 模式选择规则

- 当页面核心任务是“查数据、筛数据、批量处理数据”时，使用模式 A。
- 当页面核心任务是“查看某个对象详情，并对该对象执行操作”时，使用模式 B。
- 当页面顶部存在明确的返回路径、标题摘要和对象级操作时，优先使用模式 B。
- 当页面顶部需要承载超过 `4` 个筛选项，并且还要容纳批量操作区时，优先使用模式 A。
- 不允许把模式 A 和模式 B 的结构强行拼成一个混合怪异布局；若业务确实同时存在两类需求，应拆成“头部信息卡 + 列表筛选卡”两个区块。

#### D. Table Card 页面容器规范

- 尺寸参考：`1272px x 536px`
- 背景：`#FFFFFF`
- 边框：`1px solid #E6E7EC`
- 圆角：`16px`
- 必须设置 `overflow: hidden`
- Table Card 内部表头、表体、空状态、Hover、分页风格仍继续遵循本章和后续分页章节规则

#### E. 页面实例列宽规范

节点 `439:20862` 的页面实例推荐按以下列宽落地：

| 列              | 推荐宽度 |
| --------------- | -------- |
| Checkbox 选择列 | `42px`   |
| 默认文字列      | `200px`  |
| 图标 + 文字列   | `200px`  |
| 状态列          | `200px`  |
| 头像 + 文字列   | `200px`  |
| 开关列          | `200px`  |
| 操作列          | `230px`  |

补充规则：

- 以上列宽用于页面实例还原，可作为该页面的推荐默认列宽。
- 若业务字段变化导致列宽调整，仍必须保持表格整体左对齐、内边距、字体和 Hover 风格与本规范一致。

#### F. 页面实例单元格类型映射

- 选择列：使用本文 Checkbox 规范
- 筛选 / 查询区域：使用本文 Search、Input、Select、Dropdown 规范
- 顶部页签：使用本文 Tabs 规范
- 状态列：优先使用本文 Tag 或语义状态样式体系
- 开关列：使用本文 Switch 规范
- 操作列按钮：使用本文 Button 或链接文本规范

#### G. 冲突处理原则

- 页面规范中的“布局、层级、卡片尺寸、区域顺序、列宽”有效，应合并进最终版。
- 页面规范中的“Button、Input、Select、Dropdown、Tabs 内部字号、高度、边框、状态、交互”若与本文组件章节冲突，以本文组件章节为准。
- 页面规范中的旧表头字号、旧操作字号、旧输入控件写法，不得覆盖当前完整版规范。
- 组件设计稿与交互优先参考以下 Figma 节点：
  - Tabs：`423:122`
  - Dropdown：`421:175`
  - Select：`421:122`
  - Textarea：`404:16932`
  - Input：`404:16861`
  - Search：`404:16904`
  - Button：`404:17167`

---

## 20. Toolbar 筛选区规范

### 20.1 页面结构

标准列表页顶部必须包含：

1. 一级 Tabs 或页面标题区
2. Toolbar 筛选区
3. 批量操作区或统计区
4. Table Container
5. Pagination

对于节点 `439:20862` 对应的页面实例，页面主内容区推荐按以下顺序落地：

1. `Tabs Bar`
2. `Toolbar Card`
3. `Table Card`

层间距统一为 `12px`，主内容推荐宽度为 `1272px`。

### 20.2 Toolbar 规范

| 属性     | 值                               |
| -------- | -------------------------------- |
| 高度     | `56px`                           |
| 背景     | `#FFFFFF`                        |
| 内边距   | 建议 `12px 16px`                 |
| 项间距   | `12px`                           |
| 对齐方式 | 默认横向排列，垂直居中           |
| 换行策略 | 优先单行展示，空间不足时允许换行 |

对于页面实例 `Toolbar Card`，补充以下页面级规范：

- Toolbar Card 不是单一固定样式，至少包含两种模式：
  - 模式 A：列表筛选型双行 Toolbar Card
  - 模式 B：详情头部操作型单行 Toolbar Card
- 页面应先判断当前页面属于“列表运营型”还是“对象详情操作型”，再选择对应模式。

#### 模式 A：列表筛选型 Toolbar Card

- 外层背景：`#FFFFFF`
- 外层边框：`1px solid #E6E7EC`
- 外层圆角：`16px`
- 外层内边距：`12px`
- 内部分为上下两行
- 每行参考高度：`32px`
- 行间距：`16px`

#### 模式 B：详情头部操作型 Toolbar Card

- 参考节点：`502:7797`
- 外层背景：`#FFFFFF`
- 外层边框：`1px solid #E6E7EC`
- 外层圆角：`14px`
- 参考高度：`72px`
- 内部布局：单行横向排列
- 左区：返回按钮 + 标题区
- 右区：局部筛选 + 操作按钮
- 左右两区之间建议使用弹性布局自动分配剩余空间
- 内容应保持单行优先，不建议在此模式下做第二行批量操作

#### 模式判断标准

- 满足以下任一条件，优先使用模式 A：
  - 页面包含大量筛选项
  - 页面存在批量操作区
  - 页面主体任务是检索和处理列表数据
- 满足以下任一条件，优先使用模式 B：
  - 页面顶部需要返回上一级
  - 页面顶部要展示当前对象标题与摘要
  - 页面操作是围绕一个具体对象展开，而不是围绕整张列表展开
  - 页面仅需少量局部筛选，例如 `班级`、`年级`

### 20.3 第一行布局规范

第一行布局为“左侧筛选控件组 + 右侧操作按钮组”。

推荐筛选控件顺序：

1. 年级 `Select`
2. 当前年级 `Select`
3. 状态 `Select`
4. 招生顾问 `Select`
5. 来源 `Select`
6. 免费分类 `Select`
7. 学生姓名 `Input`
8. 手机号码 `Input`

右侧操作按钮推荐顺序：

1. 更多筛选 `Button`
2. 次按钮 `Button`
3. 主按钮 `Button`
4. 图标按钮 `Button`

补充规则：

- 上述顺序用于页面级布局参考，可按业务字段增减，但应保持“左筛选、右操作”的分组逻辑。
- 第一行中的 Button、Input、Select 必须继续遵循本文对应组件章节，不允许套用旧页面规范中的内部样式。
- 本节仅适用于模式 A，不适用于模式 B。

### 20.4 第二行布局规范

第二行布局为“左侧批量操作区 + 右侧工具操作区”。

左侧批量操作示例：

1. 通知家长
2. 分配招生顾问
3. 安排评估
4. 发送 Offer
5. 邀约缴费
6. 新生注册
7. 修改状态

右侧工具操作示例：

1. 上传图标按钮
2. 下载图标按钮
3. 视图切换图标按钮
4. 分隔线
5. 手工录入 `Button`

补充规则：

- 第二行中的图标按钮、主按钮、次按钮，仍必须使用 Button 章节定义的尺寸、圆角、padding 和状态。
- 分隔线建议高度 `12px`，颜色使用 `#E6E7EC`。
- 第二行若按钮过多，优先通过横向滚动、折叠或收纳处理，不允许压缩基础按钮高度。
- 本节仅适用于模式 A，不适用于模式 B。

### 20.5 模式 B 详情头部操作型布局规范

该模式对应 Figma 节点 `502:7797`，适用于详情页、对象页、管理页顶部工具栏。

#### A. 左侧标题区

- 结构顺序：
  1. 返回按钮
  2. 标题摘要区
- 返回按钮：
  - 建议尺寸：`32px x 32px`
  - 外边框：`1px solid #E6E7EC`
  - 形态：圆形或超大圆角胶囊按钮
  - 图标尺寸：约 `16px`
- 标题区：
  - 主标题字号：`16px`
  - 主标题行高：`24px`
  - 主标题字重：`600`
  - 主标题颜色：`#01113D`
  - 摘要字号：`12px`
  - 摘要行高：`18px`
  - 摘要字重：`400`
  - 摘要颜色：`#5A6481`
- 标题与摘要为上下两行排列

#### B. 右侧操作区

- 结构顺序建议：
  1. 少量局部 Select
  2. 竖向分隔线
  3. 主操作按钮
  4. 次操作按钮 / Quaternary 按钮
  5. Secondary 操作按钮
  6. Disabled 按钮仅在确有业务禁用态时展示
- 局部筛选项数量建议控制在 `1` 到 `3` 个
- 每个 Select 推荐宽度：`100px`
- Select 在该模式中属于表头局部筛选，优先使用 `32px` 高度
- 按钮本体仍遵循 Button 章节，不因该模式单独改写

#### C. 分隔线规范

- 用于区分“局部筛选区”和“操作按钮区”
- 高度建议：`17px` 到 `18px`
- 宽度：`1px`
- 颜色：`#E5E7EB`
- 左右留白：建议各 `4px`

#### D. 使用限制

- 模式 B 不承载批量操作区。
- 模式 B 不建议承载过多筛选项。
- 当筛选项数量增多、需要第二行操作、或需要批量处理列表数据时，应切换回模式 A。

### 20.6 筛选控件宽度规则

这一条为本次合并后的新增强制规则，适用于列表页筛选区中的 `Search / Input / Select / Dropdown`。

- 禁止在列表页筛选区中给控件写死 `256px`、`280px` 等固定宽度。
- 最小宽度必须允许缩到 `100px`。
- 建议宽度策略：
  - `min-width: 100px`
  - `width: auto`
  - `flex: 1 1 auto`
- 宽度应由 Toolbar 容器根据筛选项数量统一分配。
- 在空间允许时，尽量保证所有筛选控件在一行展示。
- 仅当容器宽度不足时，才允许控件换行。
- 组件库状态页、Figma 对照页、组件展示页仍可继续使用固定宽度：
  - `Search / Input / Textarea`：`280px`
  - `Select / Dropdown`：`256px`
- 图标按钮在页面工具栏中可固定为：
  - `32px x 32px` 页面实例尺寸
  - 但图标、圆角、边框和交互状态仍需遵循 Button 组件规范

补充说明：

- 模式 A 下，筛选控件遵循本节完整自适应规则。
- 模式 B 下，局部筛选控件通常数量较少，可使用更稳定的短宽度，如 `100px`，但其内部样式仍服从 Select / Input 组件章节。

### 20.7 页面级组件复用约束

在表格页面实例中，以下边界必须明确：

- 页面布局以页面规范为准：
  - `Tabs Bar`
  - `Toolbar Card`
  - `Table Card`
  - 分栏顺序
  - 列宽
  - 区块间距
- 内部组件以组件规范为准：
  - Button
  - Search
  - Input
  - Select
  - Dropdown
  - Tabs
  - Tag
  - Checkbox
  - Switch

这意味着：

- 可以沿用页面规范中的“哪个位置放什么控件”
- 不能沿用页面规范中已经过时的控件内部样式数值去覆盖基础组件

### 20.8 筛选区文字规范

- Label：`14px / 22px / 500 / #27345A`
- 内容：`14px / 22px / 400 / #01113D`
- Placeholder：`14px / 22px / 400 / #8C94A7`
- 辅助说明：`12px / 20px / 400 / #5A6481`

---

## 21. Pagination 规范

### 21.1 布局规则

- 位于表格容器下方
- 左侧显示区间信息
- 右侧显示页码及翻页控件
- 垂直居中对齐
- 上方与表格间距建议：`12px`
- 页面实例容器参考高度：`56px`

### 21.2 尺寸规范

| 元素                | 尺寸          |
| ------------------- | ------------- |
| 页码按钮            | `32px x 32px` |
| 上一页 / 下一页按钮 | `32px x 32px` |
| 页码间距            | `8px`         |

### 21.3 视觉规范

- 默认页码背景：透明
- 默认页码文字：`#01113D`
- 当前页背景：`#E6F1FE`
- 当前页文字：`#0445fc`
- Hover 背景：`#F2F6FF`
- 圆角：`8px`

### 21.4 文字规范

- 区间文案：`14px / 20px / 400 / #5A6481`
- 页码：`14px / 20px / 400`

---

## 22. Card List 卡片列表规范

本章节用于补充 B 端高密度列表页中的卡片式列表项规范。适用于招生计划、项目概览、任务概览、学生线索、客户线索、CRM 跟进、跟进待办等场景。卡片列表不是自由生长的展示块，而是受强约束的信息载体，必须在统一骨架、统一控高、统一操作区稳定性前提下扩展字段。

### 22.1 适用范围

- 概览型列表：计划、项目、任务、活动等单层摘要型对象
- 跟进型列表：学生线索、客户线索、任务跟进、CRM 跟进等双行信息对象
- 页面中的卡片主列表区
- 页面中的固定快捷入口卡片区

不适用于以下场景：

- 营销落地页中的自由排版内容卡片
- 信息流推荐卡片
- 允许高度自然增长的内容文章卡片
- 移动端信息瀑布流卡片

### 22.2 模板分类

卡片列表系统统一抽象为以下两类模板：

#### A. `summary-card`

适用于计划、项目、任务概览等场景。

结构字段：

- `title`
- `status`
- `metrics[]`
- `quickActions[]`
- `moreMenu`

布局特征：

- 左侧展示状态与标题
- 中部展示关键指标
- 右侧展示快捷功能入口与更多操作
- 保持单层摘要结构，不承载多行正文备注

#### B. `detail-followup-card`

适用于学生线索、客户线索、任务跟进、CRM 跟进等场景。

结构字段：

- `title`
- `statusTag`
- `metaTags[]`
- `secondaryInfoRow[]`
- `primaryAction`
- `moreMenu`

布局特征：

- 第一行承载对象身份、状态、阶段、学段、年级、年份、意向度等主识别信息
- 第二行承载负责人、跟进时间、备注摘要等补充信息
- 严格限制为两层主要内容，不允许正文扩展成第三行

### 22.3 统一设计原则

1. 所有卡片统一采用横向布局，至少包含：
   - 主信息区
   - 补充信息区
   - 操作区
2. 卡片必须控高，同一列表中的相同类型卡片高度保持一致。
3. 操作区必须固定在右侧，不能被正文、备注、长标题挤压。
4. 标题、状态、关键指标、负责人、时间等信息必须遵循统一优先级，不允许因字段增多导致骨架漂移。
5. 字段变化时只能替换内容，不得随意重组卡片骨架。
6. 所有按钮、标签、图标按钮、更多菜单必须优先复用现有设计系统组件。

### 22.4 页面级卡片列表布局规范

#### A. 卡片主列表区

| 属性       | 值                                       |
| ---------- | ---------------------------------------- |
| 布局       | `display: flex; flex-direction: column;` |
| 列表项间距 | `12px`                                   |
| 宽度       | `width: 100%`                            |
| 对齐方式   | `align-items: stretch`                   |
| 滚动方向   | 自然纵向滚动                             |

补充规则：

- 同类型卡片上下排列时必须保持统一间距 `12px`。
- 卡片列表区优先保持纵向阅读节奏，不建议使用 Masonry 或瀑布流。
- 列表容器可跟随页面自然滚动，不要求每张卡片独立滚动。

#### B. “主列表 + 右侧辅助区”页面布局

适用于“左侧卡片主列表 + 右侧快捷入口 / 辅助操作区”的业务页。

| 属性           | 值                                    |
| -------------- | ------------------------------------- |
| 布局           | `display: flex; flex-direction: row;` |
| 主次栏间距     | `16px`                                |
| 左侧主列表区   | `flex: 1 1 auto`                      |
| 右侧辅助区     | 推荐固定宽度 `320px`                  |
| 右侧辅助区定位 | 可使用 `sticky`，但不得遮挡主列表     |

补充规则：

- 右侧辅助区视觉层级低于主列表区，不得抢夺主列表阅读重心。
- 辅助区卡片的圆角、边框、按钮体系必须继续复用设计系统。

### 22.5 通用卡片容器规范

#### A. Outer Card 容器

| 属性             | 值                                    |
| ---------------- | ------------------------------------- |
| 背景             | `#FFFFFF`                             |
| 边框             | `1px solid #E6E7EC`                   |
| 圆角             | `16px`                                |
| 阴影             | 可复用页面卡片轻阴影                  |
| 内边距           | `16px`                                |
| 宽度             | `width: 100%`                         |
| 布局             | `display: flex; align-items: center;` |
| 主轴方向         | `row`                                 |
| 主区与操作区间距 | `16px`                                |

#### B. Auto Layout 映射规则

卡片的 Figma Auto Layout 在代码中必须映射为明确的容器职责：

- 外层卡片：
  - Direction：`Horizontal`
  - Align：`Center`
  - Spacing：`16px`
  - Padding：`16px`
  - Width：`Fill container`
  - Height：`Hug`，但通过 `min-height` 进行控高
- 主信息内容区：
  - `flex: 1 1 auto`
  - `min-width: 0`
  - 负责承载标题、元信息、指标
- 操作区：
  - `flex: 0 0 auto`
  - `width: hug contents`
  - 不允许 `flex-shrink`
- 分隔线：
  - `flex: 0 0 auto`
  - 高度根据对应模板固定，不随内容变化

#### C. 控高规则

- `summary-card` 推荐最小高度：`98px`
- `detail-followup-card` 推荐最小高度：`88px`
- 同一列表中的相同模板必须统一高度
- 不允许通过增加 `padding-bottom`、换行备注或多行标签让卡片高度失控

#### D. 长文本稳定性总则

卡片在真实业务场景下必须优先保证“结构稳定、信息不乱、局部不炸”，禁止依赖浏览器默认换行或默认最小内容宽度自然撑开。

强制规则：

- 左侧主信息容器必须允许收缩，并显式设置：
  - `flex: 1 1 auto`
  - `min-width: 0`
- 中部统计区必须显式设置：
  - `flex: 0 0 auto`
  - `flex-shrink: 0`
- 右侧操作区必须显式设置：
  - `flex: 0 0 auto`
  - `flex-shrink: 0`
- 当横向空间不足时，压缩优先级必须为：
  1. 先截断左侧辅助信息
  2. 再压缩左侧内部空白
  3. 仍不得压缩中部统计区
  4. 仍不得压缩右侧操作区
- 禁止因为局部长字段导致以下问题：
  - 中部统计区错位
  - 右侧操作区断裂
  - 操作名称换行
  - 图标与文字断开
  - 卡片高度突变

补充要求：

- 所有可截断文本都必须显式设置 `overflow: hidden`、`text-overflow: ellipsis`、`white-space: nowrap`，不能只依赖容器宽度碰运气。
- 若业务必须支持两行标题，必须使用严格两行截断方案，如 `line-clamp: 2` 或等价实现；禁止自然增高。
- 禁止用“隐藏 overflow 但不设 min-width: 0”的方式假装处理长文本，这在 Flex 布局中不可靠。

### 22.6 `summary-card` 规范

#### A. 结构分区

从左到右分为三段：

1. 主信息区：`status + title + meta`
2. 指标区：`metrics[]`
3. 操作区：`quickActions[] + moreMenu`

#### B. Auto Layout 结构

- Outer Card：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`16px`
  - Padding：`16px`
- Main Wrapper：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`24px`
  - Width：`Fill container`
- Primary Column：
  - Direction：`Vertical`
  - Gap：`12px`
  - Width：`Fill`，参考基础宽度 `360px`
  - `min-width: 0`
- Title Row：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`12px`
- Meta Row：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`16px`
- Metrics Group：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：默认 `44px`
  - 左 padding：建议 `32px`
  - 右 padding：建议 `8px`
- Action Group：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`8px`

#### C. 视觉与尺寸

| 元素         | 规则                          |
| ------------ | ----------------------------- |
| Title        | `16px / 24px / 500 / #01113D` |
| Meta 文案    | `14px / 20px / 400 / #5A6481` |
| Metric Value | `20px / 28px / 700 / #01113D` |
| Metric Label | `12px / 18px / 400 / #5A6481` |
| 分隔线宽度   | `1px`                         |
| 分隔线高度   | `20px`                        |

#### D. 左侧信息区稳定性规范

左侧信息区是最容易被长文本冲击的区域，必须显式约束：

- Left Info Container：
  - `flex: 1 1 auto`
  - `min-width: 0`
  - 允许收缩
- Title：
  - 默认单行显示
  - 必须设置 `min-width: 0`
  - 必须设置 `overflow: hidden`
  - 必须设置 `text-overflow: ellipsis`
  - 必须设置 `white-space: nowrap`
  - 禁止无限拉长挤压中部与右侧区域
- 若业务要求 Title 支持两行：
  - 必须使用严格两行截断
  - 必须固定卡片高度或同步校准同类卡片高度
  - 禁止自然换行把卡片撑高

元信息行如“时间、负责人、班级、年级”等，必须遵循以下规则：

- 整行必须保持单行横向排列
- 建议统一 `gap: 16px`
- 必须使用弱化层级：
  - `14px / 20px / 400 / #5A6481`
- 每个元信息项必须：
  - 使用 `inline-flex`
  - `align-items: center`
  - `min-width: 0`
- 元信息文本允许截断，但不允许：
  - 多行堆叠
  - 掉到下一行
  - 把卡片高度撑高

补充规则：

- 左侧所有辅助字段必须优先为中部统计区和右侧操作区让位。
- 当标题、负责人、班级名称都很长时，应优先截断负责人、班级等辅助字段，不得破坏卡片骨架。

#### E. 左侧次级功能项稳定性规范

像“模拟测评”这类位于左下角或左侧信息区末尾的次级操作入口，必须被视为完整功能项，而不是普通描述文本。

强制规则：

- 必须使用 `inline-flex`
- 必须设置 `align-items: center`
- 必须设置 `white-space: nowrap`
- 必须设置 `flex-shrink: 0`
- 必要时设置 `min-width: fit-content`
- 图标与文字必须始终处于同一行

绝对禁止：

- 图标和文字断开
- 文字换行后掉到第二行
- 图标掉到上一行、文字掉到下一行
- 因左侧文本过长导致该功能项被挤裂

当横向空间不足时：

- 必须优先截断左侧辅助信息
- 不得牺牲次级功能项的完整点击热区
- 不得将次级功能项降级为普通文案样式

#### F. 中部统计区稳定性规范

中部统计区必须作为独立稳定区处理，不允许被标题或元信息挤压。

强制规则：

- Stats Group：
  - `flex: 0 0 auto`
  - `flex-shrink: 0`
- 每个统计项必须上下成组：
  - 上方数值
  - 下方标签
- 统计项之间横向间距必须统一
- 所有统计项必须垂直对齐

补充规则：

- 数字是主视觉，标签是次级信息。
- 即使某个值为小数、百分比、三位数或更长位数，也不得导致：
  - 上下错位
  - 左右抖动
  - 节奏混乱
- 推荐每个统计项设置稳定最小宽度，确保不同位数下仍保持整齐分布。

#### G. 快捷入口规范

单个快捷入口建议：

| 属性             | 值                                                       |
| ---------------- | -------------------------------------------------------- |
| 可见快捷按钮上限 | `4` 个                                                   |
| 按钮主体尺寸     | 推荐 `32px x 32px`，必要时可放大到 `36px x 36px`         |
| 按钮主体形态     | 正圆形 `icon-only button`                                |
| 按钮主体边框     | `1px solid #E6E7EC`                                      |
| 按钮主体背景     | `#FFFFFF`                                                |
| 按钮主体圆角     | `50%`                                                    |
| 文本说明         | 可作为按钮下方 Caption，或通过 Tooltip / aria-label 提供 |
| 图标与文字间距   | `4px`                                                    |
| 入口之间间距     | `8px`                                                    |

补充规则：

- 快捷入口区域必须保持固定宽度，不得因标题过长压缩。
- 快捷入口区域必须视为固定结构区，禁止被左侧长文本挤压。
- 右侧快捷功能区只允许直接透出 `4` 个按钮。
- 当快捷功能数量大于 `4` 个时：
  1. 前 `4` 个高频入口保留在外层
  2. 第 `5` 个及之后的入口必须统一收进 `moreMenu`
  3. 不允许为了保留更多入口继续横向堆叠
- 可见快捷按钮的主点击热区必须是正圆形，不允许出现方形、圆角矩形、胶囊形混用。
- 若页面需要同时展示图标和文字，文字应作为按钮下方说明文案存在，不得改变按钮主体的正圆形规则。
- 图标与文字必须上下对齐，不允许图标和文字拆成两套无关布局。
- 操作名称默认单行显示，禁止换行。
- 每个操作项都必须保证：
  - 图标容器尺寸一致
  - 文字字号一致
  - 上下结构一致
  - 点击热区完整
- 禁用态操作必须保留占位，不允许直接消失导致横向节奏变化。
- “更多”作为末位独立项处理，视觉权重可略低，但尺寸体系必须与其他操作项统一。

### 22.7 `detail-followup-card` 规范

#### A. 结构分区

从左到右分为两大区域：

1. 内容区
   - 第一行：`title + statusTag + metaTags[]`
   - 第二行：`secondaryInfoRow[]`
2. 操作区
   - `primaryAction + moreMenu`

#### B. 两行强约束规则

- 第一行承载“身份与状态信息”
- 第二行承载“跟进信息与摘要信息”
- 卡片整体只允许展示两层主要内容
- 不允许出现第三行备注正文
- 不允许备注完整展开撑高卡片

#### C. Auto Layout 结构

- Outer Card：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`16px`
  - Padding：`16px`
- Content Column：
  - Direction：`Vertical`
  - Gap：`12px`
  - Width：`Fill container`
- Primary Row：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`12px`
  - Min Height：`24px`
- Secondary Row：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`0`
  - Min Height：`20px`
- Action Group：
  - Direction：`Horizontal`
  - Align：`Center`
  - Gap：`12px`
  - Width：`Hug contents`

#### D. 第二行字段规范

第二行中的字段推荐顺序：

1. 跟进人 / 负责人
2. 跟进时间 / 计划跟进 / 最近跟进
3. 备注摘要 / 跟进摘要

补充规则：

- 第二行只允许有一个“可伸缩摘要字段”
- 摘要字段使用 `flex: 1 1 auto`
- 其余字段保持 `white-space: nowrap`
- 字段之间使用竖向分隔线
- 分隔线宽度 `1px`，高度 `10px`
- 分隔线左右留白建议各 `16px`

#### E. 视觉与尺寸

| 元素           | 规则                          |
| -------------- | ----------------------------- |
| Title          | `16px / 24px / 500 / #01113D` |
| 第一行辅助信息 | `14px / 20px / 400 / #5A6481` |
| 品牌强调字段   | `14px / 20px / 500 / #0445fc` |
| 第二行 Label   | `14px / 20px / 400 / #5A6481` |
| 第二行 Value   | `14px / 20px / 400 / #01113D` |

#### F. 跟进卡片长文本稳定性规范

- Title 默认单行省略，不允许无限拉长。
- `metaTags[]` 必须保持单行横向排列，不允许换成多行堆叠。
- 第二行中的负责人、时间、摘要必须优先保持横向秩序，不允许因为摘要过长把按钮区挤坏。
- 摘要字段必须单行省略，不允许完整展示。
- 内容区必须设置 `min-width: 0`，否则摘要和标题在 Flex 布局中容易撑破右侧操作区。
- 操作区必须固定在右侧，不允许因左侧信息过长而错位。

### 22.8 文本层级与字段优先级

#### A. `summary-card`

优先级从高到低：

1. `title`
2. `status`
3. `metrics`
4. `date / owner`
5. `quickActions`

规则：

- 标题必须始终优先保留
- 指标区属于主要阅读区，不可被低优先级说明字段替代
- 日期与负责人属于辅助识别信息
- 快捷入口数量固定，避免因业务新增入口破坏布局

#### B. `detail-followup-card`

优先级从高到低：

1. `title`
2. `statusTag`
3. `意向度 / 年级 / 学段 / 年份`
4. `负责人 / 跟进时间`
5. `备注摘要`
6. 其他弱信息

规则：

- 当字段数量过多时，应优先隐藏低优先级字段
- 不得为了保留所有字段而新增第三行
- 备注默认只显示摘要，不显示完整正文

### 22.9 超长内容处理策略

#### A. 标题

- 始终单行显示
- 必须使用省略号
- 必须设置 `min-width: 0`
- 禁止标题换行撑高卡片

#### B. Meta 与辅助字段

- 日期、负责人、学段、年级、年份默认单行展示
- 当单个字段容器受限时，允许该字段文本省略
- 不允许多个辅助字段共同换行为第二层正文
- 元信息行必须保持横向单行结构，禁止为了容纳长文本拆成上下两层

#### C. 摘要字段

- 只允许在 `detail-followup-card` 第二行出现
- 默认单行省略
- 必须优先展示摘要前缀部分
- 完整备注应进入详情页、抽屉或弹窗查看

#### D. 字段裁剪策略

- 推荐为可变字段增加 `priority` 语义
- 当卡片内容溢出时：
  1. 优先保留高优先级字段
  2. 裁剪低优先级字段
  3. 最后才裁剪摘要文案长度

### 22.10 操作区稳定性规范

- 操作区必须固定在卡片右侧
- 操作区容器必须设置 `flex: 0 0 auto`
- 主按钮、图标按钮、更多菜单按钮不得因左侧内容增长而压缩宽度
- 右侧快捷按钮最多只允许透出 `4` 个，超过上限统一进入 `moreMenu`
- 右侧可见快捷按钮必须统一为正圆形图标按钮
- 禁止任何操作文案换行
- 禁止右侧图标区因左侧长文本而断裂
- 禁用态必须保留占位，不能直接消失
- `moreMenu` 建议使用 Ghost / 图标按钮形态，尺寸与系统按钮保持一致
- 危险操作必须进入确认弹窗，不允许点击后直接执行删除、关闭、结束

### 22.11 页面状态与交互规范

适用于采用卡片列表作为主体的列表页：

- 支持加载态：
  - 使用骨架屏或占位条
  - 骨架卡片高度需与真实卡片高度一致
- 支持空状态：
  - 提示当前无结果
  - 必须提供“调整筛选”或“新建”引导
- 支持错误态：
  - 展示通用错误反馈
  - 提供重试入口
- 支持分页：
  - 分页器位于列表底部右侧
  - 风格继续遵循 Pagination 章节
- 支持更多菜单：
  - 编辑、复制、结束、关闭、删除等动作统一进入菜单
  - 危险动作必须二次确认

### 22.12 组件复用与工程要求

- 卡片中的状态标签必须复用 `Tag`
- 卡片中的主按钮、Ghost 按钮、图标按钮必须复用 `Button`
- 顶部模块切换必须复用 `Tabs`
- 更多菜单的浮层样式必须复用现有浮层体系
- 卡片模板应抽离为可复用组件，不得在业务页面重复手写一份结构相似但不可复用的卡片
- 推荐将卡片模板统一收敛到：
  - `src/components/card-list-system/CardListTemplates.tsx`

### 22.13 验收清单

#### A. 结构验收

- 是否严格区分主信息区、补充信息区、操作区
- 是否所有相同模板卡片高度一致
- 是否不存在因备注过长导致的异常增高
- 是否操作区始终固定在右侧

#### B. 视觉验收

- padding、gap、圆角、边框、分隔线是否统一
- 状态标签是否复用 Tag 体系
- 卡片标题、元信息、指标值的字阶是否符合本文规则
- 快捷入口尺寸、圆形图标容器、文案间距是否统一

#### C. 交互验收

- 快捷入口是否均可点击
- 更多菜单是否正常展开与关闭
- 危险操作是否存在确认环节
- 空态、错态、加载态是否齐全

---

## 23. 可访问性规范

- 弹窗容器必须具备 `role="dialog"`。
- 弹窗标题必须通过 `aria-labelledby` 建立关联。
- 关闭按钮必须具备 `aria-label`。
- 所有交互元素必须使用原生可交互元素，如 `button`、`input`、`textarea`。
- Disabled 状态必须同步原生属性。
- Dropdown / Select 必须支持键盘聚焦和关闭。
- 焦点态不可省略，Focus 态必须清晰可见。

---

## 24. 工程实现建议

### 24.1 推荐目录

```txt
src/
  pages/
    ComponentLibraryPage.tsx
    LeadListPage.tsx
  components/
    card-list-system/
      CardListTemplates.tsx
    table-system/
      Button.tsx
      SearchField.tsx
      InputField.tsx
      SelectField.tsx
      DropdownField.tsx
      TextAreaField.tsx
      Tabs.tsx
      Tag.tsx
      TableView.tsx
      Pagination.tsx
```

### 24.2 实现要求

- 全局视觉规则优先落在 `src/index.css`。
- 组件状态通过 class 或 props 控制。
- 不允许为了业务页面复制一份新组件。
- 下拉组件必须为自定义浮层实现。
- 工具栏宽度自适应规则应由容器样式兜底，而不是写死在单个业务页面里。
- 卡片模板的 Auto Layout、控高、截断、操作区稳定性应优先沉淀为可复用基础样式，而不是散落在单个业务页面中。

---

## 25. 验收清单

### 25.1 视觉验收

- 是否与 Figma 节点的高度、圆角、颜色、间距一致。
- Button、Input、Select、Dropdown、Tabs、Tag 是否统一字阶。
- Card List 是否与节点 `582:5633`、`582:5965` 的结构逻辑一致。
- 表头是否为 `14px / 20px / 400 / #01113D`。
- 表格是否完全无斑马纹。
- Hover 是否统一使用 `#F2F6FF`。

### 25.2 交互验收

- 所有按钮是否可点击。
- Input / Search 是否可输入、可清空。
- Select / Dropdown 是否点击后再展开，而不是默认常驻展开。
- Dropdown 点击触发后是否立即呈现 Focus 态。
- 多选 Dropdown 的 Chip 是否对齐、可删除、可折叠。
- 卡片列表中的更多菜单、危险确认、分页、空态、错态是否完整。

### 25.3 列表页验收

- Toolbar 是否尽量单行展示。
- Search / Input / Select / Dropdown 是否支持 `min-width: 100px`。
- 是否不存在 `256px / 280px` 写死宽度导致的筛选区拥挤换行。
- Table Container 是否设置 `overflow: hidden`。
- Pagination 是否位置正确、风格统一。
- 同类型卡片是否保持统一高度，且操作区不被正文挤压。

---

## 26. 交付标准

- 新增组件必须补齐所有状态。
- 新增样式必须写清颜色、字号、字重、行高、圆角、边框、内边距。
- 任何视觉规范更新都必须优先合并进本文档。
- 业务页面实现与本文档冲突时，应优先修正文档或修正实现，禁止长期双标并存。

---

## 27. 使用原则

- 组件库状态页用于视觉对照时，可使用固定宽度。
- 真实业务页面，尤其是列表页 Toolbar 筛选区，必须优先遵循自适应宽度规则。
- 文字系统必须作为基础规则执行，不允许只改高度和边框，不改字阶。
- 表格规范属于强约束设计系统规范，不允许在不同业务线各自演化。
- 卡片列表页属于强约束信息结构页面，不允许为了临时业务新增字段而破坏卡片骨架、控高规则和操作区稳定性。
